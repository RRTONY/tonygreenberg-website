# TonyGreenberg.com → Next.js Migration — Master Todo

> Work through this **top to bottom, one checkbox at a time**. Check items off as they're completed.
> New Next.js codebase lives at [`web/`](web/) inside this same project. Legacy Vite/Express app
> (`client/`, `server/`, `shared/`) stays untouched and running at the project root until Phase 12 cutover.
> Companion doc: [ROUTES-INVENTORY.md](ROUTES-INVENTORY.md) — full route → source-file map used to port each page.

**Scope for this pass** (confirmed with Tony's team, 2026-08-22):
- ✅ IN: Marketing pages, Blog (121 posts), BrewSoul, PRI/Kava encyclopedias, Manifesto, all Find-Your/HumanOS assessments, FauxTony chatbot, publish-approval workflow.
- ⛔ OUT (stays on legacy stack for now, ported in a later phase): Admin dashboards, Stripe billing, spam-tracking/Spamtoast system.
- CMS boundary: **Sanity** holds editorial content only — blog posts, page copy, SEO metadata, images. Assessment questions/scoring logic and encyclopedia data stay as versioned code/config in the repo.
- **Supabase** covers auth/login, the change-request/publish-approval log, and chatbot conversation history.
- UI: Tailwind CSS v4 + shadcn/ui (Radix base) — same design-system convention the team already uses elsewhere. `next/image` for all images. No framer-motion — CSS transitions / `tailwindcss-animate` only.

---

## Phase 0 — Bootstrap

- [x] Scaffold Next.js 16 (App Router, TypeScript, Tailwind v4, `src/` dir, `@/*` import alias) at `web/`
- [x] Initialize shadcn/ui (Radix base, Nova preset, CSS variables)
- [x] Install core UI components (button, card, dialog, sheet, dropdown-menu, accordion, tabs, avatar, tooltip, separator, select, checkbox, radio-group, switch, popover, navigation-menu, badge, input, label, skeleton, progress, alert, scroll-area, carousel, aspect-ratio, hover-card, sonner, slider, toggle, toggle-group)
- [x] Install Sanity (`sanity`, `next-sanity`, `@sanity/image-url`, `@sanity/vision`, `@portabletext/react`)
- [x] Install Supabase (`@supabase/supabase-js`, `@supabase/ssr`)
- [x] Install `zod`, `react-hook-form`, `@hookform/resolvers`, `date-fns`, `next-themes`, `@anthropic-ai/sdk`, `reading-time`
- [x] Fix `pnpm-workspace.yaml` `allowBuilds` (esbuild, sharp → true)
- [x] Configure `next.config.ts` `images.remotePatterns` (Sanity CDN + legacy Manus/CloudFront hosts, temporary)
- [x] Read Next.js 16 bundled docs (`node_modules/next/dist/docs/`) for breaking changes vs. Next 13-15 (proxy.ts vs middleware.ts, Promise `params`, cache directives, Image config defaults) — confirmed, documented in agent notes
- [x] `pnpm install` clean, `pnpm dev` boots with no errors (verified: Turbopack ready in 375ms, HTTP 200)
- [x] Add `.env.local.example` documenting every env var this app needs (Sanity project ID/dataset/token, Supabase URL/anon/service keys, Anthropic API key, site URL)
- [x] `web/README.md` rewritten: what this app is, how it relates to the legacy app at the repo root, how to run it, how Sanity/Supabase are wired, Next.js 16 breaking-change notes

## Phase 1 — Sanity Studio & Schema

- [x] Connected to an existing Sanity project already owned by the team ("Tonygreenberg", project ID `a3q1cyqs`, dataset `production`) — found via an already-authenticated CLI session rather than creating a new one; recorded in `web/.env.local`
- [x] Embed Studio at `web/src/app/studio/[[...tool]]/page.tsx`
- [x] Schema: `post` (title, slug, subtitle, excerpt, pullQuote, body [portable text], heroImage, author ref, category ref, tags[], publishedAt, readTime, seo) — actual path `web/src/sanity/schemas/post.ts`
- [x] Schema: `author` (name, slug, avatar, bio)
- [x] Schema: `category` (title, slug, description)
- [x] Schema: `page` (title, slug, body, seo) — for marketing pages that need CMS-editable copy
- [x] Schema: `seo` object (metaTitle, metaDescription, keywords, ogImage, noIndex) — reused across `post`/`page`/`pageSeo`
- [x] Schema: `pageSeo` (route-keyed SEO override, not in the original plan — added because this site has far more routes than a `page` document per route would suit; see the schema file for why)
- [x] Schema: `siteSettings` singleton (site title, tagline, logo, defaultSeo, navigation, footerColumns, footerText, socialLinks)
- [x] Schema: `redirect` (source, destination, permanent) — backs the Manus→Next.js 301 map (Phase 12)
- [x] Sanity client + typed query helpers — actual path `web/src/lib/sanity/client.ts` + `queries.ts` (not `web/src/sanity/*` as originally sketched here; schemas live in `src/sanity/`, app-facing read code lives in `src/lib/sanity/`)
- [x] Sanity image URL builder helper (`web/src/lib/sanity/image.ts`) wrapping `@sanity/image-url`, used by `next/image` everywhere
- [x] Write one-off migration script (`web/scripts/migrate-blog-posts.ts`, `pnpm migrate:blog [--dry-run]`): reads `client/src/data/blogData.json` (121 posts) → creates Sanity `post` documents, idempotent by deterministic `_id` (skips posts that already exist)
- [x] Ran the migration: **121/121 posts created, 0 errors.** Author (`Tony Greenberg`) and 9 categories auto-created. 22 posts got a real hero image uploaded (17 already on RampRate's own CloudFront + 4 rescued live from the legacy `/api/img/` proxy + 1 rescued from `/manus-storage/` before Manus is decommissioned). 99 posts have no hero image yet — tracked per-post in [`BLOG-IMAGE-BRIEFS.md`](../BLOG-IMAGE-BRIEFS.md) with a suggested generation prompt for each; check them off there as real art goes in via Studio
- [ ] Spot-check 5-10 migrated posts in Studio (`/studio`) — verify body formatting (headings/lists/links) came through correctly from the markdown conversion
- [x] **Real bug found and fixed**: `markdown-to-portable-text.ts`'s list-item conversion only ever looked for a `"text"`-type token inside `item.tokens`; marked wraps any "loose" list item (one followed by a body paragraph on the next line — e.g. five-cups' "Five Questions Worth Sitting With") in a `"paragraph"` token instead, so every such item silently converted to an empty Portable Text block (visible live as a bare, contentless numbered list). Fixed the converter to handle both token shapes, then wrote `scripts/backfill-list-item-content.ts` (idempotent, patches only `body`) and ran it against all 121 already-migrated posts — **6 posts had at least one empty list item; all 6 fixed in place.**

## Phase 2 — Supabase

- [ ] **Blocked on you:** creating the actual Supabase project needs an interactive browser login (`supabase login`) or the dashboard — not something that can be done headlessly, same category of blocker as the earlier Sanity API-token issue. Either run `supabase login` yourself and say so, or create the project at supabase.com/dashboard and paste the Project URL + anon key + service-role key here (same handling as the DB password and Sanity token before — straight into `web/.env.local`, gitignored, never in a tracked file)
- [x] `web/src/lib/supabase/server.ts` (SSR client for Server Components/Actions, current `getAll`/`setAll` cookie API) and `client.ts` (browser client) helpers
- [x] `web/src/lib/supabase/service-role.ts` — bypasses RLS, server-only, for chat logging (not in the original plan, but required to satisfy "service-role write only, no public read" below)
- [x] `web/src/proxy.ts` — Supabase's required session-refresh middleware, renamed to `proxy.ts` per Next.js 16. **Ships with a guard**: if `NEXT_PUBLIC_SUPABASE_URL`/`_ANON_KEY` aren't set yet, it no-ops instead of throwing — without that guard it took the *entire site* down (every route 500'd) the moment it was added, since it ran on every request. Safe to leave in place; it activates automatically once real credentials land in `.env.local`
- [x] Auth: email/password login for internal approvers — `web/src/app/(auth)/login/page.tsx` + `login-form.tsx` + `actions.ts` (Server Action via `useActionState`, real spinner on submit per the loader rule in `CONTRIBUTING.md`, not text). Renders correctly now (verified: HTTP 200, real form) but signing in will fail until the Supabase project above actually exists
- [x] SQL migration written (`web/supabase/migrations/0001_init.sql`): `change_requests` table, `chat_conversations` + `chat_messages` tables, RLS enabled on all three. Not yet run against a live database — needs the project above first (`supabase db push` once the CLI is linked, or paste into the SQL editor)
- [x] Row-level security policies written in that same migration: authenticated-only read/write on `change_requests` (noted in-file to scope tighter than "any authenticated user" once more accounts exist); chat tables get RLS enabled with **zero** policies for `anon`/`authenticated`, which is what actually enforces "service-role write only, no public read" (service role bypasses RLS entirely — that's not a policy, it's the point)
- [ ] Seed one approver account for testing — blocked on the project existing

## Phase 3 — Shared Layout & Design System

- [x] Root layout (`web/src/app/layout.tsx`): fonts (Playfair Display / DM Mono / Source Sans 3 via `next/font/google`), `<TooltipProvider>`, theme provider (next-themes, class strategy), global `<Toaster />` (sonner), skip-to-content link
- [x] Global metadata defaults in root layout (`metadataBase`, title template, default OG/Twitter card)
- [x] Favicon (`src/app/icon.tsx`) — real brand mark, not the default Next.js icon: a generated 32×32 PNG of the "G" from the header's actual "TonyG" wordmark (Playfair Display, brand-gold), since the full wordmark doesn't read at favicon scale. Deleted the stale default `favicon.ico`.
- [x] Header/nav component (`site-header.tsx`) — real nav structure ported from legacy `Layout.tsx` (primary links, "Fix Myself"/"Explore" mega-menus, socials, theme toggle), built with shadcn `NavigationMenu`. **Deliberately not ported**: `StartHereBanner`, `ExitIntentTony`, `TonyAvatarTrigger`, `NotificationBell`, `ReadingStreakBadge`, `WhereNext` — separate behavioral/engagement features, not core layout, out of scope for this phase
- [x] Mobile nav (shadcn `Sheet` + `Accordion`, real nav content, no framer-motion)
- [x] Footer component (`site-footer.tsx`) — real content ported from `Layout.tsx`'s footer JSX (quote, contact, BioChain routing note, socials, CTAs, bio line, legal/copyright). Note: `footerData.json` turned out to be per-post article-footer data (exercise/related/riddle/CTA), not site footer content — that belongs in Phase 5, not here
- [x] Brand design tokens in `globals.css` (`--brand-gold`/`--brand-gold-light`/backgrounds/borders) ported from the real site palette (`#8B6914`, `#D4B96A`, `#0A0A10`, `#FAFAF7`, purple-tinted borders) instead of shadcn's default grayscale theme
- [x] Shared typography components — ended up built directly in Phase 5 (`portable-text.tsx`, drop-cap CSS, pull-quote block) rather than as a separate Phase 3 deliverable, since that's where they're actually consumed
- [x] `robots.ts` (AI crawlers explicitly allowed per the current SEO plan, scraper bots blocked) and `sitemap.ts` (live from Sanity post slugs already, extended per-section in later phases)
- [x] Error boundary (`error.tsx`) and `not-found.tsx` (real legacy 404 copy + countdown-redirect behavior ported from `pages/NotFound.tsx`)
- [x] Loading UI (`loading.tsx`) using shadcn `Skeleton`
- [x] Icon library decision: lucide-react for all standard UI icons (menu, sun/moon, chevrons); `react-icons/fa6` only for the X/LinkedIn brand marks, since lucide-react v1 dropped brand/logo icons entirely
- [x] Top-of-page navigation progress bar (`nav-progress-bar.tsx`) — visible feedback on route transitions, built on the Navigation API (`window.navigation`) rather than a dependency like nprogress; progressive enhancement, renders nothing where unsupported
- [ ] Verify header/footer/nav visually in a real browser (only checked via HTTP fetch + grep so far, not actually viewed)

## Phase 4 — Marketing Core Pages (30 routes)

Source: legacy `client/src/pages/*.tsx`. Port real copy/structure into Next.js pages under `web/src/app/`, styled with Tailwind + shadcn primitives, content that should be editable moved into Sanity `page` documents.

- [x] `/` (homepage) — Blog.tsx. **Not the same page as `/the-letter`** — legacy actually routes `path="/"` to the `Blog` component, not `Home.tsx` (`Home.tsx` is legacy's `/the-letter`, already ported separately below); this had briefly regressed to a plain paginated post list before this pass corrected it. Real content ported: hero (headline, stats line, essay count/nav line), Four Doors (Read/Diagnose/Engage/Verify — reusing the shared `FourDoors` component built for `/the-letter`; its icon-tile fallback path stays in `four-doors.tsx` for resilience, but all 4 real source photos are now live — see the follow-up pass note below), Editor's Picks (5 real curated posts), GemSpark Strip (a real, small editorial motif — only 3 posts in the whole corpus reference it; hand-ported subtitles since Portable Text has no raw-heading string left to regex the way legacy did), Case File banner, Latest Thinking, Positioning Statement, Core Themes (4 theme cards linking to `/?theme=X#essays-archive`, read by the archive below), Ecosystem CTA (essay count now live from Sanity instead of legacy's stale hardcoded "ninety-one"), and the full searchable/filterable archive + sidebar (Curated Journeys, Tip Me Off, Follow). **Intentionally not ported**: the ABIT waitlist email-capture form (posts to a tRPC subscribe mutation — a backend/CRM feature, out of this migration's "no backend features" scope, not a content gap) and legacy's "Most Read"/"Most Provocative" sidebar widgets (both ranked by a `reads` count that defaulted to a flat placeholder value for any post without one — not real analytics, and this migration doesn't fabricate numbers it can't back; revisit once real view-count data exists). Legacy also rendered this exact same component at `/blog` — this app briefly made `/blog` a redirect to `/` instead, then restored it as its own real page (reusing the same `HomeArchive` component under its own heading/metadata, no hero/doors/editor's-picks in front) per explicit direction to keep `/blog` and `/` as two distinct pages, matching nav's own "Read" → `/blog` link. Used lucide-react icons throughout (themes, curated journeys, doors, search bar) instead of the legacy emoji. All new sections use the site's existing theme tokens (`background`/`secondary`/`foreground`), not hardcoded colors, so they follow the light/dark toggle like every other page. Verified live: 200 OK, all sections present in SSR HTML, real post data, `/blog` redirects, lint+typecheck clean. **Two real post-ship bugs caught and fixed**: (1) `HomeArchive`'s `useState(initialTheme)` only applies on first mount — clicking a Core Themes card while already on `/` is a same-route client-side navigation that doesn't remount the component, so the theme filter silently never activated; fixed with the same "adjust state during render" pattern already used in `site-header.tsx`. (2) `site-header.tsx`'s rightmost "Explore" mega-menu (640px, `viewport={false}` on `NavigationMenu` so no built-in collision avoidance) overflowed off the right edge of the viewport on laptop-width screens; fixed by right-anchoring that one trigger's content (`right-0 left-auto`) plus a `max-w-[calc(100vw-2rem)]` safety net on both mega-menus. **Follow-up pass**: restored 4 hero animations (bowl glow pulse, spinning light rays, gold-shimmer headline text, pulsing CTA) that had been dropped despite being plain CSS `@keyframes` with no framer-motion/canvas involved — added as `--animate-*` tokens in `globals.css`, applied in `home-hero.tsx`, verified compiled in real CSS output (not just class names). Also added `NewsletterPopup` (`components/marketing/newsletter-popup.tsx`, ported from legacy `JewelPopup.tsx` + `QuirkyEmailPopup`), mounted on both `/` and `/blog` — a 3-minute-delayed glass-card email invitation; canvas particle field and rotating sacred-geometry SVG dropped (same decoration-not-worth-JS-cost reasoning), but the prismatic border/glyph-pulse/shimmer-text/CTA-pulse CSS keyframes carry over. Submitting has no backend to post to yet (Supabase pending), so it opens a pre-filled `mailto:` to the site owner instead of a fake success state, with confirmation copy that honestly says "send it" rather than claiming subscription happened. **Correction pass (caught against a screenshot of the live popup)**: the "rotating sacred-geometry SVG" and the aurora-nebula backdrop had been wrongly bundled in with the canvas particle field's drop — only the particle field (a real 60-particle `requestAnimationFrame` loop) is JS/canvas-cost; the SVG line drawing and the aurora layer are pure CSS/SVG with no loop, and are a real, visible part of the popup's identity. Restored both (`jewel-aurora-rotate`/`jewel-geo-rotate` keyframes in `globals.css`). Also fixed a real button-copy bug in the same pass: this port had "Email Me to Subscribe" where legacy's actual button reads "JOIN THE CONVERSATION". **Second follow-up pass**: found `ReturningVisitorHero` ("Welcome back…", real copy ported from legacy's own component) was never actually built despite being mounted at the very top of legacy's `Blog.tsx` — added as `components/marketing/returning-visitor-hero.tsx`. Built as a genuine improvement over the legacy approach rather than a literal port: legacy tracked visit count + last-read post in localStorage, which is client-only and would force a post-hydration flash before the banner could ever appear (the same class of hydration-mismatch bug found and fixed in `journey-bar.tsx` this same pass — see below). Tracked via real cookies instead (`tg_visit_count` incremented in `proxy.ts` on `/` requests, `tg_last_blog_slug`/`tg_last_blog_title` written by `components/blog/track-last-blog-visit.tsx` on each post view) so this Server Component reads them via `next/headers` and renders the correct final markup in the initial HTML, no client JS or flash. Legacy's `assessmentCount` CTA branch depended on a separate, unported `AssessmentProgress` localStorage store — not built for just one CTA label; always shows the real "Pick up where you left off →" fallback instead. **Real bug found and fixed while investigating**: `components/brewsoul/journey-bar.tsx` read `localStorage.getItem(...)` directly in its render body (not gated behind mount/hydration state) — a genuine hydration-mismatch bug, not just a lint nit, since the server always rendered "not visited" while the client's first hydration pass could read a different real value immediately. Fixed using the same `useSyncExternalStore` mount-gate idiom already established in `components/theme-toggle.tsx`. `components/assessments/journey-tracker.tsx`'s `useJourneyProgress` had the same underlying issue in a different shape (`useEffect` + `setState` reading localStorage, flagged by this repo's own `react-hooks/set-state-in-effect` lint rule) — rebuilt as a proper external store (`useSyncExternalStore` with a real subscribe/notify pair) instead, which also makes `markComplete`/`toggleComplete` correctly reflected across every mounted `JourneyTracker` instance, not just the one that called them. **Third follow-up pass — real images restored**: `scripts/rescue-homepage-images.ts` (a one-off rescue script already written in an earlier pass, targeting the exact 4 real door photos plus the hero) had never actually been run — re-ran it against the still-live legacy host, successfully uploaded all 5 assets to Sanity, and wired the 4 real `cdn.sanity.io` door photo URLs into `lib/content/home-doors.ts` (`img`/`imgWidth`/`imgHeight`), replacing the icon-tile fallback with the real image-first tap cards legacy always had. Verified live: all 4 real photo URLs present in rendered HTML.
- [x] `/the-letter` — Home.tsx. Real content/links/data ported (hero, Three Paths, Start Here essays, Four Doors as a shadcn `Dialog` instead of the legacy custom modal, Featured Essay pulled live from Sanity, How Tony Works, Investment Thesis, Open Questions, What I Build, Most Read, BrewSoul teaser). Deliberately not reproduced: floating-particle animation, glowing-bowl gradients, glitch text, magnetic-hover physics, typewriter intro — decorative flourishes, same judgment call as Phase 3's engagement widgets. Rescued 5 images (hero + 4 door photos) from the still-live legacy `/api/img/` proxy into Sanity before porting (`scripts/rescue-homepage-images.ts`) rather than reference the fragile path. Fixed 2 legacy bugs found while porting: `/ramprate` and `/human-os` linked to routes that don't exist (corrected to `https://ramprate.com` and `/humanos`); "Most Read"'s 5 items all pointed at the same generic `/blog` placeholder instead of each post's real slug — corrected using the real migrated post slugs. Verified live: 200 OK, real content, fixed links, real images, no leaked inline styles, lint+typecheck clean.
- [x] `/walk-through` — WalkThrough.tsx. Real content ported (7 doors, entity pills w/ real named investments, "Ask me about" lines, cross-links, pull quotes, "The Lesson" callout). Hero reuses the Tony headshot already rescued to Sanity (Phase 12 JSON-LD work) rather than re-fetching. Not ported: `AutoLinkedText`'s automatic entity hyperlinking — that's driven by the site-wide `linkMap.ts`, a genuine cross-cutting feature affecting many pages, not a one-off for this page; folded into the existing "preserve internal cross-link structure" open item in Phase 5. Verified live: 200 OK, real content, real image, correct next-page link.
- [x] `/the-territory` — Territory.tsx. Real content ported (10 heroes/influences with full notes, Operating Philosophy, pull quote, "The Lesson" callout, next-page link). Verified live: 200 OK, real content, correct next-page link.
- [x] `/engine-room` — EngineRoom.tsx. Real content ported (6 active ventures, 4 active engagements, pull quote, "The Lesson" callout, next-page link). Verified live: 200 OK, real content, correct next-page link.
- [x] `/under-nda` — UnderNDA.tsx. Real content ported (Corridor overview, pull quote, "What We're Building" list, inline quote, "The Lesson", next-page link). Verified live: 200 OK.
- [x] `/the-body` — TheBody.tsx. Real content ported (Alt Therapy Scorecard, Current Protocols, Peptide Research Tools links, pull quote, "The Lesson"). Hero image rescued from legacy `/api/img/` into Sanity before porting. Verified live: 200 OK, real image.
- [x] `/the-nightstand` — Nightstand.tsx. Real content ported (7 books, 5 key concepts, pull quote, "The Lesson"). Hero image rescued into Sanity. Verified live: 200 OK, real image.
- [x] `/the-web` — TheWeb.tsx. Real content ported (20 client badges, 4 key partnerships, pull quote, adage strip, "The Lesson"). Hero image rescued into Sanity. Verified live: 200 OK, real image.
- [x] `/pick-up-the-phone` — PickUp.tsx. Real content ported (Engagement Audit CTA to /engage, email/location, "What to Talk About" topics, social links, pull quote, "The Lesson"). Verified live: 200 OK.
- [x] `/journeys` — Journeys.tsx. Real content ported (7 curated journeys, all real stops/teasers), rebuilt as a proper shadcn `Accordion` instead of hand-rolled expand state. Fixed 2 legacy bugs: stops pointing at `/` meant the old homepage — remapped to `/the-letter` since `/` is now the blog index; 3 "Crusade Files" stops pointed at the generic `/blog` placeholder instead of the specific post — corrected to real slugs. **Also caught a real SEO issue**: shadcn's `AccordionContent` doesn't render collapsed-panel children into the DOM at all by default — every internal link on this link-heavy page was invisible to crawlers until a user clicked to expand it. Fixed with `forceMount` (documented inline). Worth checking any other page that ends up using `Accordion` for real content, not just decoration. Verified live: 200 OK, all links (including the corrected ones) present in the raw server-rendered HTML, not just after hydration.
- [x] `/recent-creations` — Portfolio.tsx. Real content ported (11 project cards, real accent colors — the one legitimate per-item inline-style exception, static Tailwind classes can't express 11 runtime hex values). Fixed a dead internal link (Homeaglow Exposed pointed at a route that doesn't exist anywhere; corrected to the real external homeaglowexposed.com already used consistently on other ported pages) and remapped `/` → `/the-letter`. Flagged but did NOT fix: "FusionRamp / STRATUM" points at `/humanos`, same as the unrelated Human OS card — looks like a copy-paste bug but there's no confirmed correct destination anywhere in the codebase, so ported as-is with a comment rather than guessed; worth a content-team check. Verified live: 200 OK, fixed links present.
- [x] `/intel` — Intel.tsx. Real content ported in full (9 portfolio companies, each with investment thesis, three gates, 7-dimension compass scores, full iRR framework, competitive landscape). Rebuilt as a shadcn `Accordion` with `forceMount` from the start (learned from `/journeys`), verified live that investment-thesis/competitor text is present in the raw SSR HTML, not just after a click. Verified live: 200 OK.
- [x] `/about` — About.tsx. Real content ported in full (3 narrative chapters, "The Receipts" stats, "What I'm Thinking Now", The Muse, Featured In, closing CTA). Built `partner-quotes.tsx` as a proper shared component (19 real client testimonials — Disney, Microsoft, eBay, Sony, etc.) since it's a cross-page resource, not a one-off; made it deterministic instead of the legacy `Math.random()` shuffle so quotes are actually in the SSR HTML. Reused the headshot + homepage hero images already rescued to Sanity. Correctly left this page's "Back to the Essays" link at `/` as-is (unlike other pages, this one genuinely means the blog/essays index). Verified live: 200 OK, both images, real quotes.
- [x] `/speaking` — Speaking.tsx. Real content ported (4 speaking topics, 4 formats, rooms spoken at, CTA). Verified live: 200 OK.
- [x] `/ecosystem` — Ecosystem.tsx. Real content ported (What It Is, Who It's For, What Members Receive, invitation CTA). Verified live: 200 OK.
- [x] `/start-here` — StartHere.tsx. Real content ported (5 curated essay cards, "After the Foundation" CTA). **Found all 5 curated essays have no real hero image** — a subset of the 99 in `BLOG-IMAGE-BRIEFS.md`, but higher priority since this is the site's own "read these first" page; renders a graceful tag-colored number panel instead of a broken image meanwhile. Verified live: 200 OK.
- [x] `/published` — Published.tsx. Real content ported in full (5 publication badges, 30 real bylines with real external URLs across 6 categories, speaking/podcast list). **Caught two real Tailwind bugs before shipping**: (1) building class names via `` `hover:${c.border}` ``/`` `${c.border}/20` `` template-literal concatenation doesn't work — Tailwind's scanner needs the complete literal string present in source, so those never would have generated any CSS; fixed by precomposing every exact variant as a full literal string. (2) mixing an all-sides `border-[color]` with a directional `border-l-4` would have fought the base `border-border` on other sides — fixed using directional `border-l-[color]` utilities instead. Verified the fix by running a full production build and grepping the compiled CSS chunk directly for `border-l-[#2563eb]/30` and `hover:border-l-[#2563eb]` — both present as real rules, not just present as strings in the HTML. Also ran `pnpm build` clean across all 16 pages shipped so far (154 static pages total) as a broader checkpoint.
- [x] `/clients` — Clients.tsx. Real content ported in full (90 real client names with industry + Hawkins-score data, real filter/sort interactivity rebuilt in a small client component, Hawkins scale legend, CTA). Caught another real React bug before shipping: `FilterButton` was defined inside `ClientsGrid`'s render body (`react-hooks/static-components` — recreates the component type every render, resetting its state); moved to module scope. Verified live: 200 OK, real client names present.
- [x] `/series` — Series.tsx ("The Collections"). Real content ported in full (11 real curated essay series with real, already-migrated post slugs, 6 legacy category colors as precomposed literal Tailwind classes per the `/published` pattern — not fragment-assembled). Episode titles/hero images now fetched live from Sanity via a new `postsBySlugsQuery` instead of the legacy static blogData.json import, so they stay in sync as posts get real hero images added. Fixed a dead legacy link: `/the-index` (not a real route, not in nav) → `/search` (the label "Search All Essays" clearly describes this app's planned search page). Verified live: 200 OK, all 11 series titles present, 49 real post links, real Sanity hero images rendering where the linked post has one.
- [x] `/framework` — LeadMagnet.tsx ("How I'd Approach Your Problem"). Real content ported in full (5-step framework, each with real description/prompt/real-world example). Rebuilt the per-step "see real-world example" toggle as shadcn `Collapsible` with `forceMount` (same crawlability rule as `/journeys`/`/intel` — verified the example text is in the raw SSR HTML, not just after a click), installed via `pnpm dlx shadcn add collapsible` since it wasn't in the project yet. **Intentionally not ported**: the email-gated "download the worksheet" form — it posted to a tRPC subscribe mutation and didn't actually attach/send a real PDF even in the legacy source (just showed a success message), a backend feature with no real deliverable behind it, out of scope. Replaced with a direct CTA to `/pick-up-the-phone`. Verified live: 200 OK, all 5 steps + real-world examples present in SSR HTML, lint+typecheck clean.
- [x] `/spirits` — Spirits.tsx ("The Liquid Library"). Real content ported in full (5 categories — Mezcal, Tequila, Sake, Coffee, Wine — each with philosophy, obsessions list, pullquote; Tequila's 5-item "Agave Matchmaker" newsletter digest; Sake's "coming soon" note). **Caught two dead external links**: the Mezcal and Tequila companion sites (SoulSmoke, LiquidSun) were hosted on `*.manus.space` — confirmed 503 along with the rest of Manus, not just the primary domain. Rendered as a non-linked "no longer live" card instead of a broken outbound link, keeping the real descriptive copy. Coffee's `/brewsoul` link kept as-is (real future internal route, Phase 6, not yet built). Verified live: 200 OK, all 5 categories + digest content present, dead external links correctly not rendered as `<a>` tags.
- [x] `/community` (+ `/find-my-tribe` → 308 redirect, `next.config.ts`) — Community.tsx. Legacy is almost entirely a functional community/CRM platform (auth-gated profile editing, CSV contact upload, email invites, member directory) backed by tRPC + a custom auth system — a backend feature, not a content page, out of this migration's scope. Ported the only real editorial content (hero copy, closing manifesto quote/link) and replaced the auth-gated CRUD tabs with an honest "still being built" note + a real CTA to `/pick-up-the-phone`, rather than reproducing a non-functional sign-in gate. Verified live: 200 OK, redirect confirmed 308.
- [x] `/engage` — Engage.tsx ("The Gate"). The qualification quiz itself (5 real questions, the exact scoring heuristic, all 3 result screens — qualified/not-ready/wrong-fit, the real Calendly link, the 2× guarantee copy) is genuinely client-side logic already in legacy (scored by a pure regex/word-count function, no server round-trip needed to decide the outcome) — ported in full as real assessment logic per the CMS-boundary rule. **Not ported**: the tRPC `engage.submit` mutation that logged each submission for Tony's own records — a lead-tracking write, out of scope; dropping it doesn't change the visitor's experience, the gate still scores and unlocks the real Calendly link correctly. Verified live: 200 OK, full quiz flow present, lint+typecheck clean.
- [x] `/amplifier` — Amplifier.tsx. Real content ported in full and unreduced (Matt Mochary note, 5 pillars, unfair advantage, 2× guarantee, 4 real pricing tiers, prep-doc requirements, "what Tony will not do" list). No backend dependency at all — every CTA is either an internal `/engage` link or a real `mailto:`. Verified live: 200 OK, all sections + both CTA link types present.
- [x] `/diamond-cut` — DiamondCut.tsx. Real content ported in full (Honest Note re: Matt Mochary, 5 cuts, 2× guarantee, 4 real pricing tiers). Legacy's 3 non-"Setting" tiers used a live `StripeCheckoutButton` — checkout/billing is deferred per this migration's scope, so those now route to `/engage` instead (pricing/copy unchanged, matching how `/amplifier`'s tiers already work), rather than wiring up a real payment flow this pass. Verified live: 200 OK, all sections + tiers present.
- [x] `/essays` — Essays.tsx. A second, distinct essay index from the homepage's archive — filters by "archetype" (Builder/Crusader/Investor, ported from `client/src/data/archetypes.ts` as `src/lib/content/archetypes.ts`) instead of Core Themes, compact list layout instead of a card grid. Not linked from current nav (legacy didn't link it from nav either — it's a real destination referenced by other not-yet-built pages like SkippyMap/CheshireGrin). Posts now fetched live from Sanity instead of the static blogData.json import. Verified live: 200 OK, archetype/category filters + real post list present.
- [x] `/invest` — Invest.tsx ("Invest in the Thesis"). Real content ported in full — 6 portfolio categories, 32 real named companies with role/note, "and many more" teaser → `/engage`, ABIT waitlist section. The waitlist is a real outbound link to impactsoul.is (confirmed live, a separate real site) — no backend/email-capture on this page at all. Verified live: 200 OK, all 6 categories + companies + outbound link present.
- [x] `/akbar` — AkbarEssay.tsx ("Los Angeles Is Losing Its Memory"). Real essay prose ported in full, unchanged (every section, every pull quote, all 6 wine pairings, the 3 reframe terms). Legacy was a ~20-image photo essay entirely on the now-fully-dead Manus `/api/img/` host — none recoverable, confirmed 404/503. Rather than fabricate placeholder imagery for a piece about specific real photographs, ported as a clean long-form article and tracked the loss explicitly (see Phase 13) instead of pretending it's not there. Kept the deliberately dark, fixed palette (this is a one-off bespoke essay design, not site chrome, so a permanent dark mood is the real intent) but reused the site's existing loaded fonts instead of the 3 extra webfonts legacy imported for just this page. Verified live: 200 OK, full essay text present.
- [x] `/living-declaration` (+ `/manifesto` → 308 redirect, `next.config.ts`) — Manifesto.tsx ("A Living Declaration — The Measurement of Becoming"). Real essay prose ported in full: the premise, "Boiling the Human" retrospective, 4 real pull quotes (Tristan Harris, Toffler, Harari, Eisenstein), the original 6 numbered practices + exceptions + specific steps, Phase 2/Balaji Srinivasan section, The Puzzle Pieces (acknowledgments), the 6 Roman-numeral principles, and the closing CTAs. Legacy's hero photo, "hand breakthrough" image, and Clarisse Abelarde's artwork photo are all on the now-fully-dead Manus host — dropped (real text credit to her kept), not fabricated. The "Ecosystem Map" 6-card grid originally linked to 6 *other* Manus-hosted micro-sites (Gem Spark, Regenerative Protocol, SoulSmoke, LiquidSun, Sacred Waters, Intimacy Assessment) plus a "Vancefolio" site — confirmed all 503; those link chips dropped, keeping only chips to pages actually live in this app (or genuinely planned, like `/humanos`/`/flow-circuit`, already referenced elsewhere in this migration). The interactive questionnaire (`trpc.manifesto.submit` — unlike `/engage`'s audit, this one has no logic that works without a backend) replaced with a direct, real `mailto:` invitation instead of a form that would go nowhere. Corrected `/assessment` → `/assessments` (the real legacy route name, confirmed in `App.tsx`; the singular looks like a typo in the original). Verified live: 200 OK, all sections + quotes present, redirect confirmed 308.
- [x] `/protecting-your-business` — ProtectingYourBusiness.tsx. Real documented fraud case file (Kristi Klawiter) ported in full — every factual claim, citation, the fact/opinion separation (`AlertBox`/`OpinionBox`), all quoted correspondence, the 13-section structure, and the full Legal Notice preserved verbatim; already-published, court-record-cited content, not new allegations authored here. **Not ported**: the "Submit Your Story" form (`trpc.cheshire.submit` — a lead-intake backend feature with no client-side logic of its own) replaced with a real `mailto:`; the closing SEO keyword-stuffing footer (bare repeated-name list, no informational content) dropped as an actual SEO improvement — modern search engines penalize that pattern, and the same keywords already live correctly in page metadata. Simplified the floating hamburger/TOC overlay to a plain inline jump-to-section list. Verified live: 200 OK, all sections + full legal notice present.
- [x] **Real bug found and fixed** (while verifying the above): a Core Themes selection (e.g. "The Crusades") often shares its display name with a real post category — showing it as a separate floating chip *next to* the category pill row (rather than merging into the matching pill) read as two disconnected, contradictory controls. Fixed `HomeArchive` so an active theme lights up the matching category pill directly (with the same click-to-clear ✕ affordance) instead of a separate chip; a theme with no same-named category still falls back to its own chip.
- [x] `/rip-peptide-sciences` — RipPeptideSciences.tsx. Real content ported in full (7-event timeline, 3 shutdown-reason cards, COA explainer, 9-vendor sortable scorecard, Tony's quote). Built `components/marketing/biochain-cta.tsx` — a shared CTA used across every peptide/bio-sourcing page, real static outbound links to RampRate's own BioChain site (confirmed live), no backend on this side. Installed `formik`+`yup` this pass for upcoming multi-field forms (peptide quiz, etc.) — documented as the house convention in `CONTRIBUTING.md`. Verified live: 200 OK, timeline/reasons/vendors/quote all present.
- [x] `/whats-legal` — WhatsLegal.tsx. Real content ported in full — 17 real compounds with real US/UK/Australia regulatory status, searchable/risk-filterable table, the 3 "key distinctions" callouts. No backend dependency. Verified live: 200 OK, full table + all real compounds present.
- [x] `/verify-your-coa` — VerifyYourCoa.tsx. Real content ported in full (interactive 7-point checklist with live score, 4-step Janoshik verification walkthrough). Extracted `components/marketing/peptide-shutdown-banner.tsx` (shared FDA-shutdown banner, was duplicated inline on `/whats-legal`, now both use the one component) and `components/marketing/coa-checklist.tsx`. Also caught and fixed 3 duplicated `EyebrowLabel` component definitions (identical code in `/amplifier`, `/diamond-cut`, `/living-declaration`) — extracted to `components/marketing/eyebrow-label.tsx`. Verified live: 200 OK, full checklist + walkthrough present.
- [x] `/price-tracker` — PriceTracker.tsx. Real content ported in full — 14 real compounds with research-vendor vs. telehealth price ranges, Tony's takes, sortable table, and the interactive up-to-4 stack cost calculator. Reused `PeptideShutdownBanner`. Verified live: 200 OK, full price table + calculator present.
- [x] `/test-your-peptides` — TestYourPeptides.tsx. Real content ported in full — 4 real independent-verification options (Janoshik, r/Peptides, Eroids, telehealth route) with real sites, costs, and Tony's notes. Reused `PeptideShutdownBanner` + `BioChainCTA`. Verified live: 200 OK, all 4 options present. Also renamed `/blog`'s heading from "The Blog" to "Essays" per feedback (matches `/essays` and the site's own voice).
- [x] `/peptide-library` — PeptideLibrary.tsx. Real content ported in full — 20 real compound profiles across 8 categories, each with mechanism, safety tier, evidence score, regulatory status, pricing, stack partners, Oura tracking, key study, and Tony's take; expandable cards with category filter. Reused `PeptideShutdownBanner` + `BioChainCTA`. Verified live: 200 OK, all 20 compounds present.
- [x] `/peptide-hall-of-shame` — PeptideHallOfShame.tsx. Real content ported in full — 20 real named clinics/providers audited on 6 weighted clinical criteria, sortable rankings with expandable weaknesses/strengths, comparison callout for the site's own assessment, methodology note. Reused `PeptideShutdownBanner` + `BioChainCTA`; built `ClinicRankings` (`components/marketing/clinic-rankings.tsx`). Real bug caught and fixed: `CRITERIA` was defined and exported from `clinic-rankings.tsx` (a `"use client"` file) and imported directly into the Server Component `page.tsx`, which crashed at runtime (`CRITERIA.map is not a function`) — a plain data re-export doesn't survive the Client→Server boundary. Fixed by moving `CRITERIA` to a new shared plain module, `lib/content/peptide-criteria.ts`, imported by both. Verified live: 200 OK, no error digest, all 20 clinic names and both methodology-card criteria present in rendered HTML.
- [x] `/peptide-supply-chain` — PeptideSupplyChain.tsx. Real content ported in full — 12 real named providers audited on a 7-category cost breakdown (manufacturing/marketing/compliance/physician/R&D/shipping/profit) with a derived credibility score, sortable rankings, expandable per-provider dollar breakdown + red-flag tags, green/red-flag guidance, methodology note. Built `SupplyChainRankings` (`components/marketing/supply-chain-rankings.tsx`) + shared `lib/content/supply-chain-providers.ts` (kept data out of the client file per the same Client→Server export pitfall fixed on `/peptide-hall-of-shame`). Dropped the legacy `EmailGate` (`trpc.subscribe.add` backend mutation) — full analysis now renders directly with no gate, matching `/peptide-library` and `/price-tracker`. Legacy hero background image (CloudFront) confirmed 403, same host/failure as `/peptide-hall-of-shame` — dropped for a CSS gradient. Reused `PeptideShutdownBanner` + `BioChainCTA`. Verified live: 200 OK, all 12 providers present.
- [x] `/peptide-matrix` — PeptideMatrix.tsx. Real content ported in full — interactive canvas scatter plot (review score vs. scientific evidence score) mapping 8 real named entities (influencers, clinics, a vendor, doctors, USADA as regulatory) across 4 quadrants, sortable/clickable entity table with selected-entity detail, 5 danger-zone psychology mechanisms, evidence-scoring methodology (crawlable via forceMount), full fair-comment/appeals legal framework. Built `PeptideMatrixExplorer` (canvas + table, client) and `PeptideMatrixMethodology` (client) + shared `lib/content/peptide-matrix.ts` (plain data module, not re-exported from either client file — same pattern as the `/peptide-hall-of-shame` CRITERIA fix). Converted every legacy emoji (🧠🔄⏰📊💰🧬🏚️📝🛡️📰✓✕▶) to lucide icons. Legacy hero background image (CloudFront) confirmed 403, same as the other peptide pages — dropped for a CSS gradient. Reused `PeptideShutdownBanner` + `BioChainCTA`. Verified live: 200 OK, all entities, quadrants, and legal-framework text present.
- [x] `/peptide-watch` — PeptideWatch.tsx. Real content ported in full across all 10 legacy sections — 3 market tiers, a 2024–26 enforcement timeline, an 18-entry wall-of-shame table with real FDA/DOJ/FTC/ITC/state/NAD citations, 12 recurring fraud patterns, 5 role-based checklists, a 10-question buyer test, a 10-category/50-point vendor scorecard, a Finnrick Analytics section, the proposed PeptideWatch Foundation, whistleblower reporting channels, and the closing "chain of custody" argument. Built `PeptideWatchFraudPatterns`, `PeptideWatchRoleChecklist`, `PeptideWatchScorecard` (all client) + shared `lib/content/peptide-watch.ts` (plain data module, same Client/Server-safe pattern as the other peptide pages). Fixed two real legacy crawlability gaps: fraud-pattern bodies and all 5 role checklists were conditionally unmounted (never in the DOM until clicked) — now always server-rendered and toggled via `hidden` instead, verified present in raw HTML. Dropped the legacy page's own duplicate `<footer>` (site already renders one global `SiteFooter`). Legacy per-section hero images (CloudFront) confirmed 403, same dead host as every other peptide page. Reused `BioChainCTA`. Verified live: 200 OK, all content present including collapsed sections.
- [x] `/quiz_25q` — PeptideQuiz25.tsx. Real content ported in full — all 25 questions across 5 scoring dimensions (Science Literacy, Red Flag Detection, Provider Evaluation, Regulatory Awareness, Personal Risk Assessment), all 4 answer choices with real feedback text per question, all 5 result profiles with real recommendations. Built `PeptideQuiz` (client component, `components/marketing/peptide-quiz.tsx`) matching the existing single-file precedent set by `engage-audit.tsx`. The email-gate phase's "GET MY REPORT" never posted anywhere even in the legacy source (no backend call in its handler) — kept as real client-only gating. `AssessmentResultActions`'s "Send to Tony"/"Keep Private" (a tRPC CRM write) was dropped; "Download PDF" (`window.print`) kept since it's real and backend-free. Legacy hero image (CloudFront) confirmed 403. Reused `PeptideShutdownBanner` + `BioChainCTA`. Verified live: 200 OK, intro copy/dimensions present, typecheck/lint/production-build clean.
- [ ] `/clock-keeper-part-2` — ClockKeeperPartII.tsx. **Deferred, not skipped.** A companion response-collection form for the `/blog/the-clock-keeper-chronicles-part-1` essay — 5 questions + alternate "modes" (reframe/meta/jazz/minimum), posts to `trpc.clockKeeper.submit`, displays a live `trpc.clockKeeper.count` response counter. Unlike `/post-intervention`, this doesn't promise anonymity, so a `mailto:` fallback (matching `newsletter-popup.tsx`'s pattern) could work for the submit action — but the live response counter has no honest fallback without a real backend. Lower priority than the rest of Phase 4 (a companion piece to one specific post, not a core marketing page); revisit with a mailto-based submit and the counter dropped.
- [x] `/charity-scorecard` + `/charity-scorecard/:slug` — CharityScorecard.tsx, CharityProfile.tsx (uses `charityData.ts`). Real content ported in full — 98 real named charities scored across 7 weighted dimensions unifying GiveWell/Charity Navigator/CharityWatch/Candid ratings plus a proprietary Cloak-vs-Clear transparency score; 3-tab explorer (Rankings with search/filter/sort/expand, Scorecard, Evaluators) and a full per-charity profile page with an SVG radar chart, score breakdown, quick facts, external ratings, and similar-charity cross-links. No backend dependency in the legacy source at all. Built `CharityScorecardExplorer` (client) + shared `components/marketing/charity-badges.tsx` (GradeBadge/CloakBadge/CloakPanel/ScoreBar/RadarChart/EvaluatorBadges, plain presentational, safe in both Server and Client contexts) + `lib/content/charity-data.ts`. `/charity-scorecard/[slug]` is a full Server Component (legacy had zero client state on that page) with `generateStaticParams` for all 98 slugs — confirmed all 98 statically generated in `pnpm build`. Fixed a real crawlability gap: the Scorecard/Evaluators tabs were conditionally unmounted until clicked (same pattern as the peptide-watch fix) — now always server-rendered, toggled via `hidden`, verified present in raw HTML. Verified live: 200 OK on both the index and a profile page, all real charity names present.
- [x] `/find-your-journey` — JourneyFinder.tsx. Real content ported in full — all 5 NLP-style questions with real weighted choices, all 6 journey archetypes (Thinker/Operator/Relationship/Body/Impact/Crusade) with real curated article lists linking to real `/blog/[slug]` posts. Built `JourneyFinder` (single client component, data + UI together, matching the `engage-audit.tsx`/`peptide-quiz.tsx` precedent). Converted legacy's plain Unicode glyph icons (◉⬡∞◈◎⚔) to lucide-react icons. Dropped the "relationship" journey's external link to `intimacyassess-tcir3hon.manus.space` — confirmed dead, same fully-decommissioned Manus host as everywhere else in this migration. `AssessmentResultActions`'s backend save dropped; retake kept (real, client-only). Verified live: 200 OK, intro copy present, typecheck/lint/production-build clean.
- [ ] `/skippy` — SkippyMap.tsx. **Deferred, not skipped.** This is a personalized "site compass" checklist page linking to ~38 destinations across every tentacle of the site (psychedelic medicine assessments, self-discovery suite, BrewSoul/Kava encyclopedias, capital/impact tools, etc.). Checked every linked route against `src/app/`: only 12 of 38 exist yet (`start-here`, `essays`, `series`, `living-declaration`, `intel`, `invest`, `charity-scorecard`, `peptide-library`, `peptide-matrix`, `verify-your-coa`, `the-body`, `about`, `ecosystem`, `community`, `clients`, `protecting-your-business`); the other 26 (`psychedelic-readiness-index`, `facilitator-index`, `friend-gate`, `post-intervention`, `dharma-finder`, `self-portrait`, `brewsoul`/`kava` encyclopedias, `impact-dashboard`, `thesis-threads`, `humanos`, `attention-theft`, `cheshire-grin`, etc.) belong to later phases not yet started. Shipping this now would mean a page that's mostly broken links. Revisit once its linked pages (or at least the Phase 4 subset — `friend-gate`, `post-intervention`, `impact-dashboard`, still on this same list) exist.
- [ ] `/ecosystem-map` — EcosystemMap.tsx. **Deferred, not skipped** — same reasoning as `/skippy`. This page is a progress-tracked site compass for the entire "Find Your Me" ecosystem: 19 assessment tools across 6 phases (`find-your-mirror`, `find-your-purpose`, `find-your-level`, `find-your-score`, `find-your-spirit`, `find-your-therapy`, `find-your-chemistry`, `find-your-water`, `find-your-partner`, `find-your-team`, `find-your-tribe`, `find-your-religion`, `find-your-mezcal`, `find-your-tequila`, `find-your-sake`, `find-your-blueprint`, `find-your-capital`, `find-your-me`). Checked `src/app/` — **none of the 19 exist yet**; they're all later-phase work. Also depends on legacy's shared `JourneyTracker`/`useJourneyProgress`/`JOURNEY_MAP` (localStorage-based progress tracking across the whole ecosystem), which hasn't been ported either since there's nothing yet to track progress through. Revisit once a meaningful chunk of the "Find Your Me" assessment suite exists.
- [ ] `/my-highlights` — MyHighlights.tsx. **Deferred, not skipped.** Entirely an authenticated account feature — gated behind `useAuth()`, reads/deletes saved text highlights via `trpc.highlights.list`/`highlights.delete`. No real content exists to show a signed-out visitor beyond a "Sign In" prompt; the underlying "select text in an essay to save it" feature isn't built anywhere else in this migration either. Blocked on real auth + a highlights backend (Phase 5+), not a content-porting task.
- [ ] `/my-impact` — MyReferrals.tsx. **Deferred, not skipped** — same reasoning as `/my-highlights`. Entirely an authenticated referral-tracking dashboard (`trpc.referral.stats`, a depth-scored referral graph). No content to port without real auth + a referrals backend.
- [ ] `/friend-gate` — FriendGate.tsx. **Deferred, not skipped.** Real backend workflow (3 tRPC calls) — generates invite links for three friends, tracks their responses, enforces a 72-hour wait. No client-side-only version makes sense; the entire feature is the backend coordination. Blocked on real auth/backend (Phase 5+).
- [ ] `/friend-survey/:token` — FriendSurvey.tsx. **Deferred, not skipped** — the token-gated survey a friend fills out after being invited via `/friend-gate` (5 tRPC calls). Depends entirely on `/friend-gate` existing first.
- [x] `/impact-dashboard` — ImpactDashboard.tsx (built, not yet pushed — pending local review). Real content ported in full across all 6 tabs (Overview, Token Ecosystems, Portfolio Impact, SoulScore, Charity Index, iRR Framework) — the 4 real ImpactSoul token ecosystems (BEYOND/REX/SPACE/BEING), 8 portfolio categories, all 12 SoulScore dimensions, the charity index summary, and the full iRR/Hawkins framework explanation. Built `ImpactDashboardExplorer` (client) + `lib/content/impact-dashboard.ts`. Dropped the canvas particle field and scroll-linked parallax hero (decoration not worth the runtime cost, same reasoning used throughout this migration) and the legacy hero images, which used the banned `/api/img/` Manus path — replaced with a plain gradient hero. The 3 "LAUNCH SOULSCORE ENGINE"/"LAUNCH C-NPV CALCULATOR" links pointed to `/soulscore`, which doesn't exist yet — rendered as a "Coming Soon" badge instead of a broken link or a silently-dropped promise. All 6 tabs render always in SSR HTML (crawlability fix, same pattern as charity-scorecard/peptide-watch) — verified live: 200 OK, all tab content present including "Coming Soon" state, typecheck/lint/production-build clean.
- [ ] `/medicine-sequencing` — MedicineSequencing.tsx. **Deferred, not skipped.** The page component itself has zero backend dependency, but it renders entirely from `MEDICINES` in `client/src/pages/pri/data.ts` — a 74KB, 39-entry dataset (each medicine has `overview`/`therapeutic`/`tradition`/`readiness`/`pricing`/`providers`/`tags`/6-dimension scores) that is also the shared foundation for `/psychedelic-readiness-index` and `/facilitator-index`, neither of which exist yet. Porting this dataset properly is a substantial task on its own, not a quick add-on to this one page — better handled as its own dedicated pass covering all three pages together than rushed here. Revisit as a unit with the other two.
- [x] `/the-philosophy` — ThePhilosophy.tsx (built, not yet pushed — pending local review). Real content ported in full — the complete "Diode of Perception" passage and all 5 "What This Means" interpretive sections. No backend dependency in the legacy source. The 4 "Instruments" links (Psychedelic Readiness Index, Facilitator Index, Medicine Sequencing, Three Friends Gate) don't exist yet — rendered as "Coming Soon" badges instead of broken links, same pattern as `/impact-dashboard`'s SoulScore links; revisit and re-link as each destination gets built. Verified live: 200 OK, full passage + interpretation present, typecheck/lint/production-build clean.
- [ ] `/post-intervention` — PostIntervention.tsx. **Deferred, not skipped.** Unlike other single-tRPC-call pages in this migration, this one can't be honestly reduced to a `mailto:` fallback: the page's entire value proposition, stated three times in its own copy ("Anonymous", "No one can see your name. No one can see your email.", results routed anonymously), is that the facilitator never sees who submitted it. A `mailto:` would expose the visitor's own email address the moment they hit send in their mail client — actively contradicting the one promise the page makes, not just a missing nice-to-have. Blocked on a real anonymous-submission backend (Phase 5+).
- [x] `/flow-circuit` — redirect stub to `flow.tonygreenberg.com` (built, not yet pushed — pending local review). Legacy did this client-side via `window.location.replace(...)` with a flash of "Loading Flow Circuit..." text; ported as a real `next.config.ts` `redirects()` entry instead — a genuine 308, no JS required, no flash. Confirmed `flow.tonygreenberg.com` is live (200) before wiring it. Verified live: `curl -I /flow-circuit` returns `308` with `location: https://flow.tonygreenberg.com/`, typecheck/lint/production-build clean.
- [x] `/supplier-intake` + `/supplier-intake-long/:token` — SupplierIntakeForm.tsx, SupplierIntakeLong.tsx (built, not yet pushed — pending local review). **Plan revised**: the original note said "keep pointed at legacy backend," but that backend (`trpc.vendor.submitStage1`/`submitStage2`, token-gated file uploads) ran on the now-fully-dead Manus host — there's nothing left to point to. Found a real, active bug in the process: 5 already-ported pages ("Become a Supply Partner" CTAs — peptide-hall-of-shame, peptide-supply-chain, peptide-matrix, whats-legal, etc.) link to `/supplier-intake`, which didn't exist, so those were live 404s. Fixed by redirecting both `/supplier-intake` and `/supplier-intake-long/:token` to RampRate's own real, live supplier intake at `ramprate.com/biochain/supplier-intake` (confirmed reachable) — the same real destination `BioChainCTA` already links to elsewhere in this migration, rather than rebuilding a form whose only working backend no longer exists. Verified live: both routes return real `308`s to the correct destination, typecheck/lint/production-build clean.
- [ ] `/shop`, `/subscribe`, `/payment-success`, `/payment-cancel` — **defer**: these are Stripe-backed; flag for the later billing-migration phase, do not port checkout logic yet, static/informational content only if ported now
- [x] Drop dead code, do not port: `HumanOS.tsx`, `ComponentShowcase.tsx`, `VendorIntakeForm.tsx`, `VendorIntakeLong.tsx`, pre-consolidation manifesto sub-pages (`AttentionEconomics.tsx`, `BlockerFinder.tsx`, `LegalDatabase.tsx`, `TenWeapons.tsx`, `ReportSpammer.tsx`). Effectively satisfied by the repo-wide flatten: the entire legacy Vite/Express app (including all of these) now lives only under `_legacy-manus-app/`, outside `src/app/` entirely — none of them are part of the deployed Next.js app or its build.

## Phase 5 — Blog Engine (Sanity-backed)

- [x] `/` and `/blog` — blog index, paginated (12/page), category filter pills, real Sanity data — verified live: 200 OK, real post links, real category links
- [x] `/blog/[slug]` — post template: hero image (`next/image`), portable text renderer, byline/reading time, pull quote (added `pullQuote` field to the `post` schema + backfilled 15 posts that had one — wasn't in the original schema, would've been silently dropped otherwise), drop caps (CSS `::first-letter` on the article's first paragraph, in `globals.css` — position-based, not per-post data, so it works for every post automatically), related posts (same-category, falling back to most-recent) — verified live against a real post: correct `<title>`, correct read time, body rendered
- [x] `/blog/category/[slug]` — category listing + pagination (not explicitly in the original phase list, but linked from the index's category pills — built so that link isn't a 404; own quality bar already requires "every page links to 2+", not dead links)
- [x] `generateStaticParams` for all post slugs and category slugs (SSG)
- [x] `sitemap.ts` already live from real Sanity data (122 URLs verified: homepage + 121 posts)
- [x] `/search` — full-text search across posts (built, not yet pushed — pending local review). No legacy `Search.tsx` ever existed — this is a genuinely new page, not a port (Phase 5's plan noted the GROQ query `searchPostsQuery` was already written but the page wasn't). Built as a pure Server Component: a plain GET `<form>` to `/search?q=...` needs no client JS at all, `sanityFetch(searchPostsQuery, ...)` runs server-side per this repo's data-fetching rule, results render with the existing `PostCard`. Was an orphan page (only reachable via `/series`' already-corrected link) — added a search icon to `site-header.tsx` (desktop + mobile) linking to `/search` so it has a real, discoverable entry point. Verified live: 200 OK empty state, `?q=trust` returns 30 real matched posts (capped by the query's own `[0...30]`), typecheck/lint/production-build clean.
- [x] `/articles` — full archive grouped by category (built, not yet pushed — pending local review). Real content ported in full — all 6 discovery-mode curations (Start Here, Short Reads, Deep Dives, Contrarian, Changed His Mind, Surprise Me) with their real curated slug lists, all 8 real category descriptions, category-grouped default view, search. Built `ArticlesExplorer` (client, `components/blog/articles-explorer.tsx`) + `lib/content/articles-discovery.ts`. Posts now come live from Sanity (`allPostsForArchiveQuery`) instead of the static `blogData.json` import. **Caught and fixed a real legacy bug**: 3 of the curated discovery-mode slugs (`psychedelics-could-become-extractive-capitalism-unless`, `enterprise-blockchain-can-big-business-co-opt-an-existing-technology`, `an-ode-to-kusaki-where-plants-become-culinary-art`) didn't match any real post slug — verified against `blogData.json`, corrected to the real slugs, same class of stale-slug bug already fixed on `/journeys`/`/recent-creations`/`/series`. Footer "SUBSCRIBE" CTA linked to `/subscribe` (Stripe-backed, deferred) — replaced with a link to `/blog`. Verified live: 200 OK, all discovery modes + categories present, both corrected slugs render as real working links, typecheck/lint/production-build clean.
- [x] `/thesis-threads` — curated-by-thread view (built, not yet pushed — pending local review). No legacy port of a data file was needed — `lib/content/thesis-threads.ts` (Tony's own newsletter-curation "content DNA" document, ported from legacy `client/src/data/sitePrompt.ts`) already existed from an earlier pass with all 69 `articleSlugs` pre-verified against the real post corpus. Built `ThesisThreadsExplorer` (client, `components/blog/thesis-threads-explorer.tsx`) + `app/thesis-threads/page.tsx` (Server Component, `sanityFetch(allPostsForArchiveQuery)`). 4 tabs ported in full: Thesis Threads (6 threads, thread selector, per-thread article list with real `THESIS_CONNECTORS` pull-quotes, unmapped-articles section), Section Map (5 section-to-thesis cards), Research Library (6 citations + 4 LP comparables), Content Rules (voice, 6 content rules, "never use" word list). Fixed legacy's `/read/[slug]` links to this site's real `/blog/[slug]` pattern. Two decorative simplifications, matching calls already made elsewhere in this migration: canvas-based floating particles dropped (same as `home-hero.tsx`), and the legacy hero's generic Unsplash stock photo + scroll-parallax dropped for a plain dark band (not a real editorial image worth rescuing into Sanity, same call as the peptide pages' dead CloudFront heroes). Was an orphan page — added to `site-nav-data.ts`'s "Read" mega-menu category, next to "All Essays". Verified live: 200 OK, stats strip shows real counts (121 essays, 69 mapped, 52 unmapped — matches the data module's own verification comment exactly), all 4 tabs render real content, typecheck/lint clean.
- [x] `/impact-futurism` — curated category view (built, not yet pushed — pending local review). Real content ported in full — all 7 real essays (3 featured + 4 more) across 5 tags (Health Systems, AI & Energy, Psychedelic Medicine, Infrastructure, Trust & Verification), curated teaser copy, tag filter. Built `ImpactFuturismExplorer` (client, `components/blog/impact-futurism-explorer.tsx`) + `lib/content/impact-futurism.ts` (curated tag/teaser only — real title/date/hero-image/read-time now come live from Sanity via `allPostsForArchiveQuery`, same content-boundary split as `articles-discovery.ts`/`thesis-threads.ts`). Legacy's per-article hero images (`/manus-storage/...` — banned Manus host — plus one dead CloudFront URL) dropped in favor of each real post's own Sanity `heroImage`, already present per post, no rescue needed. Both "Get the Dispatch" CTAs linked to `/subscribe` (Stripe-backed, deferred) — replaced with `/blog`, same fix already applied on `/articles`. Fixed legacy's stale hardcoded "All 121 Essays" to a live `posts.length`. Was an orphan page — added to `site-nav-data.ts`'s "Read" mega-menu category. Verified live: 200 OK, all 7 real titles + real Sanity hero images render, live essay count (121) confirmed in SSR HTML, typecheck/lint clean.
- [x] On-demand webhook-triggered revalidation on Sanity publish (built, not yet pushed — pending local review). **Corrected mislabel**: the old note here said "Phase 11", but Phase 11 is the unrelated Publish-Approval Workflow (Supabase dashboard) — this is its own small piece of infra, not part of that phase. Built `app/api/revalidate/route.ts`: a Route Handler using `next-sanity/webhook`'s `parseBody` (wraps `@sanity/webhook`'s `isValidSignature` — already a transitive dep via `next-sanity`, no new package needed) to verify the request against `SANITY_WEBHOOK_SECRET` (already reserved in `.env.local.example` from an earlier pass, never actually wired up). On a valid signature, calls `revalidateTag("sanity", { expire: 0 })` — every `sanityFetch()` call already tags its cache entry `"sanity"` (see `lib/sanity/client.ts`), so one tag covers every Sanity-backed page, no per-route path list to maintain. **Real Next.js 16 breaking change caught**: `revalidateTag(tag)` (single-argument) is deprecated and now a type error — the second argument is required. Read `node_modules/next/dist/docs/.../revalidateTag.md` rather than assuming the old signature; its own webhook example calls out `{ expire: 0 }` specifically for "webhooks or third-party services that need immediate expiration" (the default recommended `profile: "max"` only refreshes on next visit, wrong for this case). Still needs a human step this migration can't do: creating the actual webhook in Sanity's project dashboard (manage.sanity.io → API → Webhooks) pointing at `/api/revalidate` with a matching secret — documented in the route's own file comment. Verified live: unsigned POST → 401, bogus-signature POST → 401 (fails closed with no secret configured, correct), GET → 405, typecheck/lint clean.
- [x] Article footer standardization (built, not yet pushed — pending local review). Real content ported for all 121 entries from `footerData.json` (exercise / 2-4 curated related links with hand-written reasons / riddle / further reading), keyed by slug. Built `ArticleFooter` (`components/blog/article-footer.tsx`) + `lib/content/article-footers.ts`, wired into `app/blog/[slug]/page.tsx` after the "Read next" section. Fetched all 121 real post slugs from Sanity to cross-check every `related[].slug` reference — found and fixed 3 real stale slugs across 2 renamed posts (the Bentley/EV piece and the Butcher's Daughter piece each had a different stale slug pointing at them), same class of bug already fixed on `/journeys`/`/articles`/`/series`. Also found: (1) one entry used a `relatedArticles` key instead of `related` — the key BlogPost.tsx actually reads — meaning that post's related-links block silently never rendered even in legacy; normalized to `related`. (2) a `trueSelfCTA` field in one entry that BlogPost.tsx never reads at all (grepped — zero usages) — dead data, dropped rather than ported. (3) an `exercise.steps` checklist in one entry that legacy authored but never rendered (component only reads `exercise.title`/`.description`) — real content, so rendered here instead of repeating the oversight. The one `slug: "__manifesto__"` sentinel (legacy's in-code special case for "link to the manifesto, not a post") resolved to a real `href: "/living-declaration"` in the data itself. The hardcoded "TrueSelf Soul Print Assessment" CTA (identical on every legacy post) is kept, but repointed from `/assessments` (never existed in legacy either) to `/find-my` — the same forward-referenced assessment-hub destination every other "take an assessment" CTA in this migration already uses (tracked as its own real TODO line below, not a dead link unique to this component). Verified live on 3 real posts spanning all cases (a post with all 4 sections, the post with `exercise.steps`, and the post with the `__manifesto__` link) — all render real content, corrected links resolve, typecheck/lint clean.
- [x] Spot-check all 121 migrated posts' body formatting (automated, not a Studio visual pass — see note below on what that still leaves open). Wrote a one-off script fetching every post's `body` from Sanity and scanning for markdown-conversion artifacts (unconverted `**bold**`/`[text](url)`/headings/bullets, leaked HTML tags, empty text blocks, link marks with no `href`). Flagged 12 of 121. **Two real bugs found and one fixed**: (1) ~8 posts have `link` marks with `href: ""` — traced to the actual root cause by reading `blogData.json`'s raw `originalContent` markdown directly: the *source* markdown itself has `[text]()` links with an already-empty URL (predates this migration entirely — e.g. `the-tug-of-war-ethical-vs-economic-decisions` has `[subsidy]()`, `[dairy kills]()`, etc., empty parens in the original text). `portable-text.tsx`'s link renderer was defaulting a missing `href` to `"#"`, silently turning lost-forever data into a fake dead link on every affected post. Fixed at the renderer level (real code, not data): a link mark with no `href` now renders its text plain, with no anchor wrapper — verified live on `the-tug-of-war-ethical-vs-economic-decisions` (zero `href="#"` in the rendered HTML, previously-"linked" words like "subsidy" and "dairy kills" still read fine as plain text). (2) 4 posts (`energy-is-money-money-is-memory`, `frqncy-the-bus-that-restores-the-world`, `is-that-a-lot-clarisse-abelarde`, `you-are-the-moat`) had literal un-rendered `![alt](url)` markdown image syntax visible as plain paragraph text — the markdown→Portable-Text converter (`scripts/markdown-to-portable-text.ts`) never had image-syntax handling at all. Checked all 21 affected URLs individually (not assumed from precedent) before touching anything: 19 were genuinely dead (`/manus-storage/`, `/api/img/` — real 404s), but 2 (both in the `frqncy` post) were still-live CloudFront URLs. Since a production-content mutation is outside git and outside what this migration had done autonomously so far, held for a heads-up rather than run blind — the user then reviewed and ran it. Wrote `scripts/fix-leaked-markdown-images.ts` (kept, same one-off-migration-script convention as `rescue-homepage-images.ts`): rescues the 2 live images into Sanity as real `image` blocks (with real captions preserved as the block's remaining text), and for the 19 dead ones either drops the now-empty block entirely or strips just the dead image span while keeping any real caption prose that followed it in the same block — also pruning any `markDefs` that become unreferenced once a span is removed. Dry-run matched hand-verified expectations exactly before the real run. Verified live post-mutation (after clearing Next's stale data cache, which briefly showed pre-mutation content on the first check): 0 leaked-markdown occurrences across all 4 posts, 2 real Sanity images rendering on `frqncy-the-bus-that-restores-the-world`, all preserved captions present. Still open: an actual visual pass in `/studio` (headings/lists/links *looking* right, not just machine-parseable) hasn't been done for any post beyond the original 1 curled at the top of this phase — this scan catches structural artifacts, not visual fidelity.
- [x] Reconcile `blogData.json` vs. the legacy `blogPosts.ts` module. **Resolved — not a duplicate.** Traced `blogPosts.ts`'s own header comment ("Blog Post Data — 20 most recent posts... original excerpt, AI-rewritten 'Updated for Today' version") plus its actual 25-entry `BlogPost[]` array: this is a distinct, unrelated, much smaller dataset (an AI-rewrite/"TonyG Format" exercise with `lesson`/`nextSteps` fields) that was never the input to `migrate-blog-posts.ts`, which reads only `blogData.json` (confirmed in that script's own top-of-file comment and its `LegacyPost` type). `blogData.json` is the sole authoritative source for the 121-post corpus; `blogPosts.ts` was never a candidate alternate and needs no reconciliation — it's simply out of scope, superseded implicitly by the real 121-post migration.
- [x] Preserve internal cross-link structure from `client/src/data/linkMap.ts` / `readingPaths.ts` (built, not yet pushed — pending local review). Both ported as real, working features, not just cross-referenced.
  - **`readingPaths.ts`** (90+ hand-picked "read next" connections with real editorial reasons) → `lib/content/reading-paths.ts`, wired into `/blog/[slug]`'s existing "Read next" section: a curated path now takes priority over the old always-generic same-category/recent fallback (which still applies for the ~6 posts with no curated path), and each recommended card now shows its real reason line. Extracted the actual runtime object via `tsx` (safer than regex-parsing hand-written TS) and cross-checked all 117 keys + 352 target slugs against the real 121-post corpus: found 2 keys and 2 targets using stale slug spellings for posts already renamed elsewhere in this migration (same Bentley/EV and Butcher's Daughter posts already fixed in the article-footers pass) — fixed. The 2 stale keys turned out to be duplicate entries for posts that already had a correctly-spelled key elsewhere with its own different recommendation list; kept the correctly-spelled key's original list rather than merging two competing editorial takes. Fixing one of those also surfaced a self-reference (a post recommending itself once its alias resolved to its own slug) — dropped. 8 recommendations pointing at `attention-theft-the-silent-crime-of-the-digital-age` dropped — not a blog post (legacy's own un-ported standalone manifesto page), so these already silently never rendered in legacy either (its lookup only ever matched against blog posts). Also fixed a real pre-existing gap noticed while touching this code: `relatedPostsQuery`/`recentPostsQuery` never selected `publishedAt`, so the fallback "Read next" cards were silently missing their date. Verified live on 3 real posts (a curated one, the merged/self-ref-fixed one, and an uncurated fallback one) — real reasons render, no self-links, fallback still works.
  - **`linkMap.ts`** (~190 real entities auto-linked wherever named in body text) → `lib/content/link-map.ts` (data + ported `autoLinkSegments`) + `lib/sanity/auto-link-body.ts` (`autoLinkBody`, a Portable-Text-block transform run on `post.body` before it reaches `<PortableText>` in `/blog/[slug]/page.tsx` — deliberately done as a data-tree transform rather than changes to `portable-text.tsx`'s renderer, so the existing `marks.link` handler picks up new links with zero renderer changes and zero risk to the link-mark fix made earlier this phase). Conservative by design: only touches spans with no marks at all (bold/italic/code/already-linked text is untouched), and skips a whole block if it already has a real link mark anywhere in it — a per-span/per-block version of legacy's own per-paragraph "skip if markdown links already present" rule. Dropped 17 of 193 entries before porting: they point at internal routes that don't exist yet in this app (`/self-portrait`, `/ecosystem-map`, `/the-index`, the 5 `/brewsoul/*` pages, 2 `/assessments/*` pages, `/the-mirror`, `/homeaglow-...`) — auto-linking those would manufacture a fresh 404 on every matching mention across all 121 posts, a much larger blast radius than this migration's one deliberate `/find-my` forward-reference (kept, since it's already used elsewhere in this app and tracked as a real upcoming page). Checked the 2 `.manus.space` external links (Clarisse Abelarde's and Aqueous's own sites) live — both 200 OK, kept; the Zero-Manus-dependency rule is about this project's own dead CDN paths, not linking out to a third party's page-builder-hosted site. Verified live on 3 real posts: real new external links landed with correct hrefs (e.g. `harvard.edu`, `linkedin.com/in/alexveytsel`) using the transform's own key-naming convention to confirm they're genuinely new (not pre-existing), a post with zero matching entities correctly got zero new links, and a post's pre-existing real link (Alison Zai's site) rendered completely untouched — confirming the "don't touch already-marked spans" guarantee holds.

## Phase 6 — BrewSoul (28 routes)

Source: `client/src/pages/brewsoul/*`, data from `brewsoul-coffees-expanded.ts`, `brewsoul-encyclopedia.ts`, `brewsoul-chains.ts`, `cityData.ts`, `wallOfShameData.ts`. Port as Next.js pages under `web/src/app/brewsoul/`; encyclopedia/coffee data stays as typed `.ts` modules in the new codebase (not Sanity, per CMS-boundary decision).

- [x] `/brewsoul` — welcome/landing gate (built, not yet pushed — pending local review), along with the shared foundation the rest of Phase 6 depends on. Ported `BrewSoulLayout.tsx` → `app/brewsoul/layout.tsx` + `components/brewsoul/brewsoul-nav.tsx` (`BrewSoulNav`/`BrewSoulFooter`), `CategoryBadge.tsx` → `components/brewsoul/category-badge.tsx`, `brewsoul-config.ts` → `lib/content/brewsoul-config.ts`, and `BrewSoulDirectory.tsx`'s `CATEGORIES`/`PAGE_CATEGORY_MAP` data (not the page itself yet) → `lib/content/brewsoul-directory.ts`. **Real architecture decision**: BrewSoul is a self-contained sub-site with its own nav/footer/coffee-brown palette, same as legacy — but this app has one flat root layout with no route groups, so the main site's `SiteHeader`/`SiteFooter` needed to be suppressed for this one subtree without restructuring every existing route. Added `components/site-chrome.tsx` (`SiteChrome`, a small client wrapper checking `pathname.startsWith("/brewsoul")`) around both in `app/layout.tsx` — verified via literal `<header>`/`<footer>`/`<nav>` tag counts in real SSR HTML (not text search, which false-positives on Next dev mode's RSC debug payload) that `/brewsoul` gets zero main-site chrome and its own real nav+footer, while `/` still gets exactly one of each. Dropped `TonyAvatarTrigger` (FauxTony chatbot widget — Phase 10, no backend yet, same "don't fake it" call made elsewhere) from the nav. Ported `BrewSoulWelcome.tsx` → `components/brewsoul/brewsoul-welcome-quiz.tsx` (8-screen onboarding quiz, real identity-scoring logic, palate questions, gear recs) + thin `app/brewsoul/page.tsx`. Two simplifications: canvas-based `WarmParticles` dropped (same decoration-not-worth-JS-cost call made repeatedly elsewhere), and the dead hero photo (`/api/img/brewsoul-orig_84eb4bc9.jpg`, confirmed 404) dropped for a CSS gradient — the cheap scroll-linked parallax itself was kept. `/find-your-sake` kept as a forward-reference in the ecosystem links, same precedent as `/find-my`. Fixed a real stale hardcoded count (legacy's footer said "See All 24 Pages" — the actual `CATEGORIES` array sums to 25 — now computed live via `BREWSOUL_TOTAL_PAGES`). Verified live: 200 OK, real quiz copy renders, typecheck/lint clean (needed a `next typegen` run for the brand-new route's types, per this repo's own known PageProps/LayoutProps quirk).
- [x] `/brewsoul/first-sip` — onboarding intro (built, not yet pushed — pending local review). Ported "The First Sip Is a Vote" in full — the complete manifesto essay (5 sections, 3 pullquotes, the couplets block, the disclosure-demands grid), the 5-tool journey map, 6 related-essay links (real slugs, cross-checked against this session's already-verified corpus), the closing + sources, and the email capture. Built as a Server Component (all static except the email form) + `components/brewsoul/next-steps.tsx` (shared "what's next" CTA, itself a Server Component — legacy's hover effects were plain CSS-able, no JS state needed) + `components/brewsoul/first-sip-email-capture.tsx` (client island, mailto: fallback since no subscribe backend exists, same pattern as `newsletter-popup.tsx`). Dropped the canvas `CoffeeDust` decoration and the confirmed-dead hero photo (`/api/img/brewsoul-orig_84eb4bc9.jpg`, 404, same Manus proxy) — with no image left, the scroll-linked parallax that moved it went too. The 5 tool-card links (prescription/health/pairings/economics/compare) point at later Phase 6 routes not built yet — same forward-reference pattern as `/find-my`. Verified live: 200 OK, full essay text present, `CategoryBadge` correctly shows "Start Here" (from `PAGE_CATEGORY_MAP`), zero main-site chrome leaking in, typecheck/lint clean.
- [x] `/brewsoul/home` — the real BrewSoul dashboard (distinct from `/brewsoul`'s identity-quiz welcome gate and `/brewsoul/first-sip`'s manifesto): hero + stats, Follow the Dollar teaser, Top QPR coffees (computed live via `computeQPR`), the 2025 crisis callout, New Drops, a Wall of Shame preview (`SHAME_ENTRIES`), the research-hub card grid, and a cyclable health-fact ticker (`components/brewsoul/health-ticker.tsx`, its own small client island so the rest of the page stays a Server Component). Canvas `WarmParticles`/`ParallaxBg` decorations and the dead hero photo (`/api/img/brewsoul-orig_84eb4bc9.jpg`, same confirmed-404 Manus proxy used elsewhere in Phase 6) dropped for CSS gradients. Stats use the real live `BREWSOUL_COFFEES.length` (107) in place of legacy's stale "103". Verified live: 200 OK.
- [x] `/brewsoul/browse` — coffee listing (`components/brewsoul/browse-explorer.tsx`), real filter/search over the full `BREWSOUL_COFFEES` catalog. Verified live: 200 OK.
- [x] `/brewsoul/coffee/[id]` — coffee detail page (breadcrumb, QPR + tier header, quick stats, tasting notes, flavor-profile bars, the 4 intelligence scores, transparency/ethics data, buy links, competition wins, similar-coffees cross-links, journey CTAs), server-rendered with `generateStaticParams` over all 107 real coffees (this repo's bounded-dynamic-route convention, same as `/blog/[slug]`) instead of legacy's client-side `useParams` lookup. Verified live: 200 OK.
- [x] `/brewsoul/quiz` — matching quiz (built, not yet pushed — pending local review), along with the real data/engine foundation most of the rest of Phase 6 depends on. Ported `lib/intelligence-engine/{types,scoring}.ts` verbatim (generic scoring engine — QPR/availability/scarcity/wow/tier/freshness — reusable beyond BrewSoul) and the full coffee catalog (legacy's 3 files: `brewsoul-coffees.ts` + `-expanded.ts` + `-expanded2.ts`, merged at import time) → one `lib/content/brewsoul-coffees.ts`, extracted via `tsx` (not hand-typed — 107 real entries, ~6.5K lines). **Caught a real stale count**: legacy's own file header claimed "105" and its UI copy said "103", but the actual merged arrays contain 107 — this migration shows the live `BREWSOUL_COFFEES.length` everywhere instead of a repeated hardcoded number. Ported `JourneyBar.tsx` (`components/brewsoul/journey-bar.tsx`) — the fixed bottom "what's next" bar shown on every BrewSoul content page except the identity quiz — and retrofitted it onto `/brewsoul/first-sip` too (built before JourneyBar existed). Built `components/brewsoul/brewsoul-quiz.tsx`: real 7-question taste quiz scored against each coffee's real `flavorProfile` via the real scoring engine, ranking the full catalog by match. Legacy's `useMarkVisited` was renamed to `markVisited` (it's a plain localStorage write, not a hook — its old `use`-prefixed name tripped React's rules-of-hooks lint when called from inside a `useEffect`). Dropped the canvas `WarmParticles` decoration and the dead hero photo (same confirmed-404 Manus proxy, same call as `/brewsoul` and `/brewsoul/first-sip`). Verified live: 200 OK, real question copy renders, live coffee count (107) shown correctly, correct "Start Here" category badge, correct chrome, typecheck/lint clean.
- [x] `/brewsoul/wall-of-shame` — content page. Real 13 entries (brand/category/evidence/severity/response) from `SHAME_ENTRIES`, unchanged. Category filter buttons extracted into `components/brewsoul/shame-explorer.tsx` (the only interactive piece) so the page itself stays a Server Component. Verified live: 200 OK.
- [x] `/brewsoul/follow-the-dollar` — supply-chain essay. Real `DOLLAR_BREAKDOWNS` segment bars (farmer/exporter/importer/roaster/retailer %, real commentary), unchanged. The farmer-segment bar's data-driven color (green/gold/red by threshold) is a small known set of variants — precomposed as 3 literal Tailwind classes rather than an inline `style` background, per this repo's dynamic-class rule; only the genuinely per-item width stays as `style`. Fully static, Server Component. Verified live: 200 OK.
- [x] `/brewsoul/farms` — sourcing content ("Farm Passports"). Real `FARMS` entries (country/region/producer/altitude/story/varieties/certifications), unchanged. Fully static. Verified live: 200 OK.
- [x] `/brewsoul/mold-free` — mycotoxin content. Real verified/claims/untested split computed live over the full `BREWSOUL_COFFEES` catalog by `moldTestStatus`. Fully static. Verified live: 200 OK.
- [x] `/brewsoul/experiences` — experiential content ("Experience Map"). Real `EXPERIENCES` entries (farm tours, cafés, cupping labs, roastery visits), unchanged. Country filter buttons extracted into `components/brewsoul/experiences-explorer.tsx`, same split-out-the-interactive-bit pattern as wall-of-shame. Verified live: 200 OK.
- [x] `/brewsoul/health` — health deep-dive, ported from `BrewSoulHealthFull.tsx` (the file legacy's own router actually uses for this route — `BrewSoulContent.tsx`'s same-named `BrewSoulHealth` export is real but genuinely dead/unrouted code in legacy, shadowed by this one). Real 8 longevity findings with real citations/participant counts, 7 real risks with severity ratings, CYP1A2 caffeine-metabolism genetics, a 6-step chronobiology timing schedule, 8 real bioactive compounds, 3 real mycotoxin threats, and a 3-step "simplification protocol," all ported unchanged and verbatim. Dead Manus hero image dropped for a CSS gradient. Legacy's own redundant `BrewSoulLayout` wrapper dropped — `app/brewsoul/layout.tsx` already wraps every route. The two real interactive pieces (show-all-risks toggle, per-compound click-to-expand) split into `components/brewsoul/health-{risks,compounds}.tsx` client islands so the rest stays a Server Component. Legacy's bespoke earthy "science" palette (soil/clay/sage/moss — distinct from the rest of BrewSoul's amber/gold commercial palette) kept as a deliberate one-off sub-identity for this one page, same reasoning as `/attention-theft`'s own "crusade" palette elsewhere in this migration. Verified live: 200 OK, typecheck/lint clean.
- [x] `/brewsoul/varieties` — reference: varieties. Real `VARIETIES` encyclopedia entries (species/origin/genetics/cup profile/yield/disease resistance), unchanged. Search extracted into `components/brewsoul/varieties-explorer.tsx`. Verified live: 200 OK.
- [x] `/brewsoul/processing` — reference: processing. Real `PROCESSING_METHODS` entries (description/flavor impact/complexity/controversy), unchanged. Fully static. Verified live: 200 OK.
- [x] `/brewsoul/roasters` — reference: roaster directory. Real `ROASTERS` entries (philosophy/quality/transparency/farmer-equity scores), unchanged. Search + tier filter extracted into `components/brewsoul/roasters-explorer.tsx`. Verified live: 200 OK.
- [x] `/brewsoul/glossary` — reference: glossary. Real `GLOSSARY` terms, unchanged. Search + alphabetical grouping extracted into `components/brewsoul/glossary-explorer.tsx`. Verified live: 200 OK.
- [x] `/brewsoul/pairings` — reference: pairings. Real `PAIRINGS` (food/drink pairings by origin) and 3 real curated `FLIGHTS` (each linking to real coffees in the catalog), unchanged. Fully static. Verified live: 200 OK.
- [x] `/brewsoul/economics` — reference: economics. Real live-computed stats (coffee/origin/roaster/variety counts, average price, average farmer share via `DOLLAR_BREAKDOWNS`), a real price-distribution histogram, and a real top-origins ranking — all computed directly from `BREWSOUL_COFFEES` at render time, unchanged. Fully static. Verified live: 200 OK.
- [x] `/brewsoul/compare` — tool: compare coffees. Real side-by-side comparison of up to 4 coffees across origin/producer/variety/process/price/cupping and the 4 intelligence scores (QPR/availability/wow/overall), unchanged. Interactivity in `components/brewsoul/compare-explorer.tsx`. Verified live: 200 OK.
- [x] `/brewsoul/blend-builder` — tool: blend builder. Real combine-up-to-5-coffees ratio calculator with weighted-average QPR/Wow/Overall scores via range sliders, unchanged (never persisted anywhere in legacy either — a pure in-session calculator). Interactivity in `components/brewsoul/blend-builder-explorer.tsx`. Verified live: 200 OK.
- [x] `/brewsoul/drops` — tool: product drops ("Latest Drops"). Real top-12-by-overall-score coffees. No client-side state at all (legacy's own `useMemo` had no changing dependencies — a pure derived list), so this ships as a plain Server Component. Verified live: 200 OK.
- [x] `/brewsoul/collection` — tool: personal collection tracker ("My Collection"). Real save/toggle-to-collection behavior, unchanged. **Real bug found and fixed**: legacy read `localStorage` directly inside a `useState` initializer — unsafe during SSR, same hydration-mismatch class already fixed in `journey-bar.tsx`/`journey-tracker.tsx` — rebuilt around a module-level external store fed through `useSyncExternalStore` in `components/brewsoul/collection-explorer.tsx`. **Also found and fixed**: the already-ported `brewsoul-directory.ts` listed this page's path as `/brewsoul/my-coffees` — a real dead link in legacy's own directory (legacy's actual router only ever served this at `/brewsoul/collection`, `/brewsoul/my-coffees` never existed) — corrected to the real path. Verified live: 200 OK.
- [x] `/brewsoul/submit` — tool: submit a coffee ("Submit or Appeal"). Real submit-a-coffee / appeal-a-score form, unchanged in structure. **Real bug found and fixed**: legacy's own comment admitted "In production this would POST to a tRPC endpoint" but in the meantime wrote the submission to `localStorage` and showed "Received. We'll review your submission" regardless — actively misleading, since nobody would ever see a `localStorage`-only write. Replaced with a real `mailto:` fallback pre-filled with the form fields, same honest-degradation pattern as `first-sip-email-capture.tsx` — no fake success state. Verified live: 200 OK.
- [x] `/brewsoul/prescription` — personalized "prescription", ported from `BrewSoulPrescriptionFull.tsx` (the file legacy's router actually uses — `BrewSoulTools.tsx`'s same-named export is dead/unrouted, same shadowed-file trap as `/brewsoul/health`). Real 5-step questionnaire (genetics/timing/health/flavor/budget) generating a real, deterministic prescription (`generatePrescription` — real roast/cup-count/brand/pricing/warning logic), all ported unchanged and verbatim. Legacy's `trpc.coffeePrescription.generate` AI-enhancement mutation doesn't exist in this migration — dropped along with its "AI ANALYSIS LOADING..." block, which would otherwise spin forever and never resolve; the real prescription itself never depended on that call, it's already fully computed client-side. Same bespoke earthy "science" palette as `/brewsoul/health` kept as the same deliberate sub-identity. Verified live: 200 OK, typecheck/lint clean.
- [x] `/brewsoul/chains` — "Top 100 Coffee Chains" ranking (not supply-chain visualization — that's `/brewsoul/economics`/`/brewsoul/follow-the-dollar`; this is chain rankings). Real 100-chain dataset extracted from legacy's computed `CHAIN_RANKINGS` export (5 real weighted dimensions → aggregate → S-F tier) via a one-off `tsx` script (same approach as `brewsoul-coffees.ts`) into `lib/content/brewsoul-chains.ts`, with a new `ChainEntry` type added to `intelligence-engine/types.ts`. Real tier/type filters, search, and per-chain expand, unchanged, extracted into `components/brewsoul/chains-explorer.tsx`. Verified live: 200 OK, typecheck/lint clean.
- [x] `/brewsoul/biodynamic` — biodynamic content. The complete real census of Demeter-certified biodynamic coffee: 3 real source farms, 6 real roasters/brands with real product tables and pricing, real nutritional-science compounds, a real cost calculator, and a real impact/tokenization section, all ported unchanged and verbatim. Dead Manus hero image dropped for a CSS gradient. The 3 genuinely interactive pieces (click-to-expand farms/roasters, cups-per-day cost slider) split into `components/brewsoul/{biodynamic-farms,biodynamic-roasters,cost-calculator}.tsx` client islands so the rest stays a Server Component. Same bespoke earthy "science" palette as `/brewsoul/health` and `/brewsoul/prescription` kept as the same deliberate sub-identity. Verified live: 200 OK, typecheck/lint clean.
- [x] `/brewsoul/decaf` — decaf content ("Paint Stripper or Pure Water?"). Real 3-process breakdown (methylene chloride/Swiss Water/supercritical CO₂), 13 real clean-decaf brands ("Hall of Fame"), 10 real methylene-chloride brands ("Wall of Shame," with real Clean Label Project lawsuit citations), a real carbon footprint comparison, real decaf health science, and a real price-comparison table, all ported unchanged and verbatim. Legacy's manual `document.title`/`<meta keywords>` mutation in a `useEffect` (a real anti-pattern — Next's `generateMetadata` already owns the head) dropped for the real `metadata` export. **Real bug found and fixed**: the 13-brand `FAME` dataset was initially exported from the "use client" `decaf-fame.tsx` component file and imported into the Server Component page for its price-comparison table — non-component exports from a client module aren't available across the RSC boundary, which threw `FAME is not iterable` at runtime (only caught by live verification, not typecheck/lint). Moved the data to a plain `lib/content/brewsoul-decaf.ts` module instead. The 2 genuinely interactive pieces (click-to-expand Hall of Fame, reveal-toggle Wall of Shame) split into `components/brewsoul/decaf-{fame,shame}.tsx` client islands. Same bespoke earthy "science" palette as the other `/brewsoul` deep-dive pages kept as the same deliberate sub-identity. Verified live: 200 OK, typecheck/lint clean.
- [x] `/brewsoul/esoteric` — rare-coffee content ("Esoteric Coffee Index"). Real 9-coffee index across 2 tiers (Panama Geisha, Yemen Mokha, Liberica, Eugenioides, Wush Wush, Sidra, Pink Bourbon, Bourbon Aji, Sudan Rume) with real tasting notes, rarity scores, and real named buy sources, plus a real acquisition-priority list and explorer scorecard, all ported unchanged and verbatim. **Real content gap found, not fixed**: legacy's own "Top 10 Acquisition Priority" list and scorecard table both reference "Laurina" as a 10th entry, but no coffee card for it exists anywhere in the source dataset — ported as-is rather than inventing card content that was never written. Zero client-side state anywhere in this page (only CSS-able hover) — ships entirely as a Server Component. Verified live: 200 OK, typecheck/lint clean.
- [x] `/brewsoul/cities` + `/brewsoul/cities/[slug]` — city directory + detail ("City Intelligence Module"). Real 25-city leaderboard (name/state/grade/score/tagline/verdict/live stats), a real 7-dimension scoring methodology, and real national + universal/nuanced chain report cards, all ported unchanged and verbatim into `lib/content/brewsoul-cities.ts`. **Real data-completeness note, not a bug**: legacy's own comment admits only Portland has full shop-by-shop data (25 real best + 5 real worst shops with real blurbs/scores/order-this/skip-this) — the other 24 cities have real city-level stats but empty shop/chain/bridge-drink/non-coffee arrays; legacy's own UI already handles this honestly (a real "Full data coming soon" state on both the card and detail page, not fake data) — preserved exactly, verified for both Portland (full tabs) and Seattle (coming-soon state). Grade filter, methodology modal, and chain-toggle interactivity in `components/brewsoul/cities-explorer.tsx`; per-city tab switching and per-shop expand in `components/brewsoul/city-detail-tabs.tsx`. Detail route uses `generateStaticParams` over all 25 real slugs. Scroll-linked parallax and floating-particle backgrounds dropped, same decoration call made throughout this migration. **Real image-hosting gap flagged, not silently worked around**: the 25 city hero photos are real, live Unsplash URLs (not a dead Manus proxy) but aren't yet uploaded to Sanity per this repo's own image convention — added `images.unsplash.com` to `next.config.ts`'s `remotePatterns` as an interim measure rather than blocking the port on a 25-photo Sanity migration; tracked here as a real follow-up. Verified live: 200 OK on the index, Portland (full data), and Seattle (coming-soon state), typecheck/lint clean.
- [x] `/brewsoul/directory` — real master page index ("Everything Inside BrewSoul," not a roaster directory — that's `/brewsoul/roasters`). Real 6 categories with real page descriptions/icons, unchanged; the underlying data already lived in `lib/content/brewsoul-directory.ts` (built earlier this phase to power `CategoryBadge`) — this route is where it finally becomes a real browsable page. Category filter extracted into `components/brewsoul/directory-explorer.tsx`. Fixed the stale "Browse All 103 Coffees" label (real count is 107) by dropping the hardcoded number from the shared data module — benefits every other consumer of that module too. Verified live: 200 OK, typecheck/lint clean.
- [x] `/brewsoul/guest` + `/brewsoul/guest/shanita-nicholas` — guest series index + profile ("Tony G Guest Series"). Real single guest entry (Shanita Nicholas — real headline/teaser/tags) on the index, and the real 10-question interview on the detail page (Fair Trade economics, roasting mythology, the C Market, regenerative vs. sustainable, origin-roasting economics vs. Fair Trade, blockchain traceability, pre-harvest financing — real substantive answers with real citations), all ported unchanged and verbatim into `components/brewsoul/shanita-interview.tsx`. Legacy's guest photo (`/api/img/brewsoul-orig_84eb4bc9.jpg`) was the same dead Manus proxy confirmed 404 throughout this migration — dropped for a plain initial avatar on both pages rather than a broken headshot. Per-question expand/collapse, "Expand/Collapse All," and the real Twitter/LinkedIn/copy-link share row are genuinely interactive, split into client islands. Verified live: 200 OK on both, typecheck/lint clean.

**Phase 6 — BrewSoul is now fully complete.** All 28 routes ported, verified live, typecheck/lint clean.
- [x] Shared: port `BrewSoulLayout`, `CategoryBadge`, `JourneyBar`, `NextSteps` as Next.js layout/components — `app/brewsoul/layout.tsx` + `components/brewsoul/{brewsoul-nav,category-badge,journey-bar,next-steps}.tsx`, all built and reused by every `/brewsoul/*` route above.

## Phase 7 — PRI & Kava Encyclopedias (19 routes)

- [x] Kava shared foundation: `app/kava/layout.tsx` + `components/kava/kava-nav.tsx` (`KavaNav`/`KavaFooter`, 10 real nav items) + `components/kava/kava-ui.tsx` (`KavaHero`/`KavaSection`/`KavaSectionTitle`/`KavaCard`/`KavaBadge`/`KavaDivider`/`KavaDisclaimer`, converted from legacy inline `style={}` to Tailwind + new `--kava-*` tokens in `globals.css`). Added `/kava` to `site-chrome.tsx`'s `SUPPRESSED_PREFIXES` (self-contained sub-site, same pattern as BrewSoul and `/attention-theft`). Real dead link fixed: legacy nav pointed at `/pri`, which legacy's own `App.tsx` never routed (only `/psychedelic-readiness-index` exists) — corrected.
- [x] `/kava` (home) — 9 module cards, "Threshold Doctrine," "Sacred Masculine Triad" (Mezcal/Kava/Peyote), 4 market stats, all ported unchanged. Verified live: 200 OK, exactly 1 header/1 footer (chrome suppression confirmed), typecheck/lint clean.
- [x] `/kava/origins` — real migration timeline (6 Pacific nations, ~3000 BCE–1000 CE), real 6-island selector (Vanuatu/Fiji/Tonga/Samoa/Hawaii/Pohnpei, each with real facts/ceremony/cultivar/safety content), and the real Noble vs. Tudei safety comparison, all ported unchanged and verbatim. Island-selector interactivity in `components/kava/origins-explorer.tsx`. Verified live: 200 OK, typecheck/lint clean.
- [x] `/kava/interactions` — real 24-substance drug interaction database (real severity ratings across an 11-value scale, real pharmacological mechanisms, real PRI protocol guidance) and real 5-enzyme CYP450 inhibition profile, ported unchanged and verbatim. Search + severity-filter interactivity in `components/kava/interactions-explorer.tsx`, using a precomposed `SEVERITY_CONFIG` class lookup (not string-built classes — same Tailwind static-scanner rule enforced elsewhere this migration). Legacy's unused `Download` icon import (dead code, no export feature attached to it) not ported. Verified live: 200 OK, typecheck/lint clean.
- [x] `/kava/science` — real 6 major kavalactones (real receptor mechanisms, real PRI relevance) and real 11-item combined pharmacological profile, ported unchanged into `lib/content/kava-science.ts`. **RSC-boundary bug caught proactively before shipping**: initially drafted `KAVALACTONES` as an export of the `"use client"` `chemotype-decoder.tsx`, which would have thrown the same `TypeError: X is not iterable` already hit once this migration on `/brewsoul/decaf` when the Server Component page tried to import it — moved to the plain data module before ever running it. Chemotype decoder tool (enter e.g. `4-2-6-5-3-1`, see dominant kavalactones) kept as the real interactive client island. Verified live: 200 OK, typecheck/lint clean.
- [x] `/kava/assessment` — real 5-domain, 20-question PRI readiness assessment (somatic/psychological/relational/cultural/integration), real per-domain point weights, and real 4-band recommendation scoring (CLEARED/CONDITIONAL/DEFER/NOT READY), all ported unchanged into `components/kava/assessment-quiz.tsx`. Fully stateful (domain navigation, per-question answers, results view), so kept as a single client island per this migration's established pattern for multi-step assessments. Domain and recommendation accent colors (5 + 4 distinct hexes, none matching the `--kava-*` brand palette) precomposed into literal `DOMAIN_STYLES`/`RECOMMENDATION_STYLES` lookup objects rather than `style={}`, avoiding the dynamic-class bug already caught twice this migration (`chains-explorer.tsx`, `biodynamic/page.tsx`). **Real backend gap, handled honestly**: legacy's `AssessmentResultActions` took `sessionId`/serialized-`answers` props feeding a `trpc.assessments.submit` mutation this migration never built — used the already-simplified, print-only `AssessmentResultActions` (`components/assessments/result-actions.tsx`, built during Phase 9) instead of reproducing a submit call with no backend. Verified live: 200 OK, real domain/question content confirmed in rendered HTML, typecheck/lint clean.
- [x] `/kava/hawaii` — real ICE/meth crisis statistics (Quest Diagnostics 410% figure, Hawaii DOH conviction rates, law enforcement seizure data), real dual crisis/revival timeline (1985–present), the real (explicitly-labeled hypothesis, not proven) pharmacological alignment table, and real recovery resource listings, all ported unchanged and verbatim. Fully static, no client island needed. Verified live: 200 OK, typecheck/lint clean.
- [x] `/kava/myths` — real 7-myth evidence review (2002/2014 European kava ban and reversal, WHO findings, CYP2D6 metabolism differences by population, chemotype/preparation distinctions) and the real closing takeaway, ported unchanged. Expand/collapse interactivity in `components/kava/myths-explorer.tsx`, verdict colors (3-value enum) precomposed into a literal `VERDICT_CLASS` lookup. Extended shared `KavaCard` (`kava-ui.tsx`) with an optional `onClick` prop to support this — first Kava page needing a clickable card. Verified live: 200 OK, typecheck/lint clean.
- [x] `/kava/caffeine` — real CYP1A2 inhibition mechanism (56%, the same pathway grapefruit juice uses on medications), real caffeine cutoff protocol, and the real kava-vs-coffee comparison table, ported unchanged. Fully static, no client island needed. Verified live: 200 OK, typecheck/lint clean.
- [x] `/kava/products` — real 5-category product index (ceremonial powder, fresh frozen, RTD beverages, supplements, kava bars) with real named vendors/cultivars/chemotypes/pricing/PRI ratings and real outbound buy links (all live vendor domains, not Manus proxies — no image assets on this page to check), ported unchanged into `components/kava/products-explorer.tsx`. Category-tab interactivity kept as the client island. Verified live: 200 OK, typecheck/lint clean.
- [x] `/kava/certification` — real 12-module, 80-hour facilitator curriculum, real 3-tier certification structure (Practitioner/Threshold Facilitator/Master Facilitator), and real prerequisites list, ported unchanged. Fully static, no client island needed; legacy's `wouter` cross-links replaced with `next/link`. Verified live: 200 OK, typecheck/lint clean.

**Phase 7a — Kava is now fully complete.** All 10 `/kava/*` routes ported, verified live, typecheck/lint clean.
- [x] PRI shared data: `lib/content/pri-data.ts` (copied verbatim from legacy `pri/data.ts` — real 39-medicine pharmacopoeia, 50+ questions across 6 domains, MAO-B interaction matrix, medication interaction guide, crisis/integration resources, pathway questions), `lib/content/pri-safety-data.ts` (verbatim safety data merge source), `lib/content/pri-medicine-images.ts` (verbatim real CloudFront hero-image map, all spot-checked 200 OK — RampRate's own CDN, already whitelisted, not Manus), `lib/content/pri-shared.ts` (the real scoring engine — `computeScores`/`READINESS_LEVELS`/`getMedicineWithSafety` — extracted to a plain module per the RSC-boundary lesson from `/brewsoul/decaf` and `/kava/science`). Added `--pri-*` CSS custom properties to `globals.css` (this subsystem's own purple/parchment/near-black palette, same pattern as `--kava-*`/`--crusade-*`) since Phase 7's PRI section spans 9 more routes.
- [x] `/psychedelic-readiness-index` (core tool) — the real hero, 39-medicine grid with compare/save/bookmark tooling, MAO-B interaction matrix, medication interaction guide, crisis resources, the real 5-state assessment flow (philosophy intro → readiness quiz → pathway & access questions → legal disclaimer gate → results), medicine detail modal (overview/safety/correction tabs), testimonials, and ImpactSoul cross-promotion, all ported unchanged and verbatim into `components/pri/psychedelic-readiness-index.tsx` plus supporting client islands (`disclaimer-gate.tsx`, `medicine-modal.tsx`, `safety-section.tsx`, `correction-form.tsx`, `share-bar.tsx`, `cinematic-band.tsx`). Kept as one client island — a single `state` machine drives which section renders, same call made for other large multi-step assessments this migration. Real hero/section photography (8 CloudFront images) kept via `next/image`; legacy's purely decorative glitch-scanline overlay divs and CSS shimmer/glow `@keyframes` dropped (`no framer-motion` — CONTRIBUTING.md rule 5 — and "decoration not worth the cost," same call made throughout this migration).
  - **Real stale-count bug found and fixed**: legacy's own hardcoded copy ("26 plant medicines," "26 Sacred Medicines," stats-bar "26") never caught up to the dataset's real growth to 39 entries — replaced every instance with `MEDICINES.length` so it can't drift again.
  - **Three unbuilt-backend mutations found and handled honestly**: `trpc.pri.submitConsent`, `trpc.pri.submitCorrection`, and `trpc.assessments.submit` (referral request) all had no backend in this migration. Consent is still recorded locally via `sessionStorage` (matching legacy's own actual gating behavior — its mutation failure was already silently ignored). The correction form and referral form now compose real `mailto:` messages to Tony instead — legacy's correction-form error path was a genuine fake-success bug (`catch { setSubmitted(true) }`, "Still show success for UX" even when the mutation threw), not reproduced.
  - **Real dead-code dropped, not carried forward**: the inert "Add my results to the research pool" checkbox (no handler, no effect, in legacy either) is removed rather than shipped as a checkbox that visibly does nothing; legacy's duplicate "download results" affordance (a full custom PDF-export button immediately followed by a second, plainer `<AssessmentResultActions>` call) is collapsed to just the real, fully-featured export.
  - **Real stale share URL fixed**: legacy's share bar pointed at `onlytimebuystrust.com`; corrected to this site's real canonical domain.
  - **Hydration-safety fix applied twice**: legacy read `sessionStorage`/`localStorage` directly inside `useState` initializers (`hasConsent`, `savedMeds`) — unsafe during SSR and a real hydration-mismatch source, same class of bug fixed elsewhere this migration — rebuilt around module-level external stores fed through `useSyncExternalStore`. Legacy's two `useEffect`+`setState` calls (post-consent pending-medicine restore; `?m=` deep-link auto-open) were also rewritten as "adjust state during render" instead, per `react-hooks/set-state-in-effect` and the exact pattern CONTRIBUTING.md's own example table prescribes.
  - Verified live: 200 OK, real content (medicine grid, MAO-B matrix, testimonials, philosophy section) confirmed in rendered HTML, exactly 1 header/1 footer (uses the site's normal chrome — legacy never wrapped PRI pages in a suppressed-chrome sub-layout, unlike Kava/BrewSoul), typecheck/lint clean.
- [x] PRI: `/peyote-mescaline` — MescalineDeepDive, real receptor pharmacology table, the real "Latuda Mirror" same-target-opposite-action comparison, real clinical outcomes (Agin-Liebes et al. 2021, n=452), the real 16-row pharma-to-plant alternatives table, real supplement protocol (pre/day-of/post-integration), real mescaline-specific PRI dimension scores, the real medicine-selector comparison table, and real sources, all ported unchanged and verbatim into `lib/content/pri-mescaline-module.ts` (copied verbatim) + `app/peyote-mescaline/page.tsx`. Fully static except the pharma-table expand toggle, extracted into `components/pri/pharma-table.tsx`; shared `PriSection`/`PriEyebrow` wrapper (`components/pri/pri-section.tsx`) introduced here for reuse by the remaining PRI deep-dive modules. All 8 real section photos (CloudFront) spot-checked live before use; legacy's purely decorative glitch-scanline overlays dropped. Verified live: 200 OK, real content confirmed in rendered HTML, exactly 1 header/1 footer, typecheck/lint clean.
- [x] PRI: `/iboga-ibogaine` — IbogaDeepDive, "The Plant vs The Isolate": real alkaloid-profile comparison, real receptor pharmacology, real head-to-head iboga-vs-ibogaine table, real clinical outcomes (Mash/Noller/Schenberg/Brown & Alper/Cherian/Williams — including the 2024 Stanford Nature Medicine SOF-veteran data and the 2026 19,071-patient safety analysis), real pharma-to-plant alternatives, real supplement protocol, real dimension thresholds, real medicine selector, Tony's real first-hand account of sitting ibogaine at The Mission Within, real hard-stop contraindications, a real 9-facility directory (with the 2 facilities legacy explicitly removed and why), real Nagoya Protocol / extractive-capitalism argument, real 2026 US legal-status rundown, the embedded Iboga Compass v2 documentation section (`components/pri/iboga-compass-section.tsx`, its own client island for the question-group toggles), and real sources — all ported unchanged and verbatim into `lib/content/pri-iboga-module.ts` + `lib/content/pri-iboga-compass-data.ts` (both copied verbatim) + `app/iboga-ibogaine/page.tsx`. Fully static except the pharma-table expand toggle (`components/pri/iboga-pharma-table.tsx`) and the compass section's toggles; reused the shared `PriSection`/`PriEyebrow` wrapper from the mescaline module. All 5 real hero/section photos (CloudFront) spot-checked live before use. Verified live: 200 OK, real content confirmed in rendered HTML, exactly 1 header/1 footer, typecheck/lint clean.
- [x] PRI: `/iboga-compass` — IbogaCompassAssessment, the real interactive 28-question / 7-section compass (outcome/medical/approach/logistics/geography/personal/aftercare), the real drag-or-arrow priority-rank step (top dimension ×2.0, bottom ×0.5), the real facility-scoring engine, and the real results view (matched facilities with real "why this matched" reasoning and real elimination reasons), all ported unchanged and verbatim. `lib/content/pri-iboga-compass-engine.ts` (real scoring logic — weight deltas, hard filters, substance bonuses, priority-rank application, facility scoring) and `lib/content/pri-iboga-facility-data.ts` (real facility directory + substance-bonus data) both copied verbatim from legacy rather than hand-transcribed, given this is the actual matching algorithm — correctness matters more here than anywhere else in this migration. Uses this site's own `brand-gold`/`foreground`/`muted-foreground` design tokens rather than the `pri-*` palette — legacy's own hex here (`#8B6914`) is an exact match for `brand-gold`, confirming this one page always used the main site identity, not a PRI-specific one. **Real backend gap handled honestly**: legacy's lead-capture gate posted a rich structured payload (question-by-question answers, top facility matches, primary goal, budget range) to `trpc.assessments.submit` (no backend built for this migration) — replaced with a `mailto:` to Tony carrying the same structured summary, same pattern used throughout this migration; the meaningless "Submitting..." pending state and its error-toast path were dropped along with it. Verified live: 200 OK, real question/section content confirmed in rendered HTML, exactly 1 header/1 footer, typecheck/lint clean.
- [x] PRI: `/pri-calibration` — forced-rank calibration study, the real Bradley-Terry pairwise-comparison model (all 15 unique pairs of the 6 PRI dimensions), the real drag-or-arrow full ranking step, real methodology copy (acquiescence-bias explanation, the 4 headline validation stats), and the real Monte Carlo validation metrics (`lib/content/pri-efficacy-data.json`, copied verbatim from legacy — also feeds `/pri-efficacy` below), all ported unchanged into `components/pri/calibration-assessment.tsx`. **Real backend gap, handled honestly**: legacy's "Contribute to Research?" opt-in phase posted to `trpc.priCalibration.submit` (no backend built for this migration), and the results view's "Community Data" section read a live `trpc.priCalibration.stats` aggregate query showing other users' scores — neither has an honest backend-free equivalent (a cross-user aggregate can't be faked locally, and a research opt-in toggle with nothing behind it would be dishonest UI, the same call already made dropping the main PRI results page's inert research-pool checkbox). Both dropped; the flow goes straight from ranking to results. Verified live: 200 OK, real content confirmed in rendered HTML, exactly 1 header/1 footer, typecheck/lint clean.
- [x] PRI: `/pri-efficacy` — psychometric validation dashboard, the real Monte Carlo simulation results (10,000 synthetic respondents), real classification-accuracy comparison, real dimension-priority distribution (the real first-dimension-inflation finding), real test-retest reliability and entropy metrics, and the real methodology writeup, all ported unchanged into `app/pri-efficacy/page.tsx`, reading the same `lib/content/pri-efficacy-data.json` also used by `/pri-calibration`. **Real backend gap, handled honestly**: legacy's "Live Calibrations" tab read a live `trpc.priCalibration.stats` query — since `/pri-calibration`'s own submit call was already dropped (no backend, see that page's port note), this tab would permanently render "0 total calibrations, no data yet," a dead feature masquerading as a live one. Dropped entirely rather than ship a perpetually-empty tab; only the real, fully-populated Simulation view ships — which also means this page has no state left at all, so it's a plain Server Component now instead of a client one. Verified live: 200 OK, real content confirmed in rendered HTML, exactly 1 header/1 footer, typecheck/lint clean.
- [ ] PRI: `/pri-research` — **Deferred, not skipped** (access-model question resolved by inspection). Read the real legacy source: this is a genuine admin-only dashboard (`useAuth()` gated to `role === "admin"`) showing `trpc.priCalibration.researchData` — the *same* calibration-submission backend already confirmed absent and intentionally not built for `/pri-calibration` and `/pri-efficacy` (see their port notes). No admin-auth system exists anywhere in this Next.js migration yet either. So this isn't just an access-control decision — even fully wired up, the page would have zero real data to show, forever. Not worth building an admin dashboard against a backend this migration has twice already decided not to build. Added to "Explicitly deferred" below alongside the other admin dashboards; revisit only if/when both a real admin-auth system and a real calibration-submission backend exist.
- [x] PRI: `/facilitator-index` — "The Facilitator Index: Know Who You Go With," the real 15-question quick-intake archetype quiz (real Bradley-Terry-style axis derivation, fully client-computed via `deriveArchetype()`), the real 108-item, 12-band (plus Band 0) practitioner self-assessment instrument, and the real methodology prose (rationale, code/tier system, data-handling policy, Reciprocity Gate, Corroboration token system), all ported unchanged and verbatim into `lib/content/facilitator-index-data.ts` (single authoritative content source) + `components/facilitator/*` + `app/facilitator-index/page.tsx`. **Real content-drift bug found and fixed**: legacy shipped two divergent copies of the same "108 items" — the on-page JSX and a separate `printFacilitatorQuestions()` print/PDF export using an older, different set of item numbers and band groupings (missing the on-page "Twenty-Five Traditions and Protocols" band entirely) — the print export was never updated when the on-page instrument was revised. Used the on-page version (the one actually shown to visitors) as the single source of truth and rebuilt the print export to generate from that same data, so the two can never diverge again. **Real fake-data found and dropped**: legacy's `PeerStats` component's own comment admits its numbers ("163 practitioners mapped so far," etc.) are "seeded, realistic-feeling" — fabricated, not real — dropped entirely, same principle as BrewSoul's stale "reads" counts; `UsageCounter`'s hardcoded 160-practitioner fallback (shown forever since no backend populates the real query) dropped for the same reason. **Real backend gaps, handled honestly**: `trpc.facilitatorIndex.submit` (both the quick-intake log and the full 108-item `SubmissionForm`) has no backend — replaced with real `mailto:` submissions carrying the same fields; `trpc.facilitatorIndex.getVotes`/`vote` (`VotingSection`, a live cross-user vote tally) has no honest local equivalent — dropped, same reasoning as the cross-user aggregates dropped from `/pri-calibration` and `/pri-efficacy`. **Real hostile-UX pattern dropped, not ported**: legacy's page-wide `copy`/`contextmenu` event blockers (billed as "IP protection") provide no real security (trivially bypassed) while frustrating legitimate visitors — not reproduced; the decorative copyright watermark SVG overlay is kept since it's cheap and harmless. Two in-body Manus `/api/img/` section images dropped per the zero-Manus rule (tracked as a Phase 13 image gap); the real hero photo (CloudFront) is kept. Verified live: 200 OK, real content confirmed in rendered HTML, exactly 1 header/1 footer, typecheck clean.
- [x] Port shared safety/medicine data modules (`safety-data.ts`, `medicine-images.ts`) as typed `.ts` content in the new codebase — done as part of the main PRI page above (`lib/content/pri-safety-data.ts`, `lib/content/pri-medicine-images.ts`, both copied verbatim).

**Phase 7 — PRI & Kava Encyclopedias is now fully complete.** All 19 routes ported (10 Kava + 9 PRI), verified live, typecheck clean; `/pri-research` explicitly deferred (see its own line above and "Explicitly deferred" below) rather than skipped silently.

## Phase 8 — Attention Theft Manifesto (7 routes)

- [x] `/attention-theft` — consolidated mega-page. Real content ported in full across all 9 sections (hero, The Sacred, The Heresy economics data + full breakdown table + sources, the 10 Weapons, Legal Arsenal w/ filter + letter template, AI Blocker Finder quiz, Report a Spammer, closing vow + related reading) — `AttentionTheft.tsx` used as the single source, the 5 pre-consolidation sub-pages not resurrected (they never existed in this codebase anyway). Section "glamour" photography is real, live imagery on RampRate's own CloudFront (already whitelisted in `next.config.ts` as a non-Manus, permanent host — confirmed all 7 URLs 200 before using them), rendered via `next/image`. **Real backend gap found and handled honestly**: legacy's Wall of Shame (a live `trpc.spam.wallOfShame` ranked list) and the Report form's `trpc.spam.submit` both depended on a spam-tracking backend that this migration explicitly defers (see "Explicitly deferred" list below: "`/youve-been-reported`, `/spamtoast`, admin spam tooling") — rather than fake a public database with no real data behind it, the report form now composes a `mailto:` to Tony (same honest non-fake-success pattern as `newsletter-popup.tsx`), and the ranked Wall of Shame list isn't reproduced. Two simplifications matching precedent set elsewhere in this migration: legacy's "Fraunces" serif (loaded for this one page only) was dropped for `font-heading` (Playfair Display, already loaded — same call made on `/akbar`), and the glitch/VHS/ember-particle/shattered-glass decoration layer was dropped (same "decoration not worth the cost" judgment applied to canvas particles and glitch text throughout this migration). **Real mid-build correction**: initial pass used raw hex Tailwind arbitrary values (`bg-[#FAFAF7]`, `text-[#8B6914]`, etc.) for this page's "crusade" red/gold/teal accent palette — caught that `#FAFAF7`/`#8B6914` are exact duplicates of the already-tokenized `--background`/`--brand-gold`, and that the genuinely new colors (red/crimson/teal/ember/ink/muted/brown) had no token at all. Fixed by adding real `--crusade-*` CSS custom properties to `globals.css` (theme-independent, same pattern as `--brand-gold`) and wiring them into the `@theme` block so `text-crusade-red`/`bg-crusade-teal`/etc. compile to real `var(--crusade-*)` rules — verified in the compiled CSS chunk, not just as class-name text in HTML. Verified live: 200 OK, real content in all sections, exactly one header/footer (no main-site chrome leak), all 7 images rendering, `?offender=` deep-link banner works, typecheck/lint clean.
- [x] Legacy anchor redirects: `/attention-theft/economics`, `/blocker-finder`, `/legal`, `/weapons`, `/report` → `/attention-theft#<anchor>` via `next.config.ts` `redirects()`. Verified live: all return real 308s to the correct anchor.
- [x] Port `ManifestoLayout` scroll-tracking wrapper behavior — `components/manifesto/manifesto-nav.tsx` tracks which section is in view on scroll and highlights the matching nav pill (desktop + mobile), real behavior kept per this line item rather than simplified away.

## Phase 9 — Assessments (33 routes)

Logic (questions, scoring, branching) stays as code per the CMS-boundary decision — port as typed data modules, not Sanity documents.

- [x] HumanOS section — `/humanos`, `/humanos/philosophy`, `/humanos/ecosystem`, `/humanos/resources`, `/humanos/connect`, `/humanos/path-to-here`, ported from `_legacy-manus-app/client/src/pages/HumanOS.tsx` + `humanos/*.tsx`. The top-level `HumanOS.tsx` (an iframe embed of an external `manus.space` site) turned out to be dead code, never wired into legacy's router — the real, routed implementation is `HumanosHome`/`HumanosPhilosophy`/`HumanosEcosystem`/`HumanosResources`/`HumanosConnect`/`HumanosPathToHere` behind `HumanosLayout`, which is what got ported, with its own nested `layout.tsx` (route now added to `site-chrome.tsx`'s `SUPPRESSED_PREFIXES` so the main `SiteHeader`/`SiteFooter` don't double up around HumanOS's own nav/footer). Real content in full: the Maximizer-vs-Satisficer framework and "Boiling the Human"/"Maximizer Trap"/"Conscious Satisficer" philosophy sections, the full ecosystem roster (1 mentor, 8 advisors, 3 partners, 29 clients), all 4 transformation playbooks + 3 daily-practice protocols + 8-book reading list, all 4 connect channels, and the 6-entry "Path to Here" timeline (2010 H+ Summit through 2025). Real bugs fixed: every "Take the Diagnostic" CTA site-wide pointed at `/assessment`, an 8-question Maximizer/Satisficer quiz that was never actually built anywhere (legacy's real `/assessment` and `/assessments` routes are two different, unrelated quiz sets) — redirected to `/humanos/philosophy`, which delivers the actual promised content; Connect's "Read the Living Declaration" pointed at `/manifesto`, a dead redirect stub in legacy's own router — pointed directly at `/living-declaration`; the footer's "Investment Portfolio" pointed at `/portfolio`, which never existed in legacy's router at all — fixed to `/invest`. Every `/api/img/`-hosted asset (hero photo, nav logo, all 12 person headshots) was dropped per the zero-Manus rule — nav uses a text/icon wordmark, hero uses a CSS gradient, people use shadcn `Avatar` initials-fallback cards instead of broken images; the Satisficer-diagram and Consciousness-Scale images were kept via `next/image` since they live on the already-allowlisted `d2xsxph8kpxj0f.cloudfront.net` host used elsewhere in this app, not a Manus host. Also dropped: a fake "Diagnostics Completed" counter and "Someone just discovered they're a Maximizer" toast, both fabricated client-side `Math.random()` activity with no real backing data. `/humanos` was already forward-referenced from `/living-declaration`, `/recent-creations`, `/the-letter`, and `site-nav-data.ts` before this page existed. Verified live: all 6 routes 200 OK, real content confirmed, fixed link targets confirmed 200, typecheck/lint clean.
- [x] `/find-your-me` (+ `/find-my-me`, `/discover`) — "Find My — To Find Your We," the ecosystem hub assessment and by far the largest page in Phase 9. Real 5 NLP-driven questions, 6 dimensions (relationships/purpose/body/truth/tribe/consciousness), and 6 real archetypes (each with a real description, real blog article links, real satellite-site links, real "go deeper" forward-references, and a real share text) ported unchanged and verbatim into a new `lib/content/find-your-me.ts` data module (kept separate from the component given the file's exceptional size, same pattern as `brewsoul-directory.ts`). All 17 unique blog article slugs referenced across the 6 archetypes were individually verified live (200 OK) against this migration's real Sanity blog before porting — zero dead article links. **Real accuracy fix**: legacy's "Full Ecosystem Directory" (26 items across 5 categories, with a real LIVE/COMING pill UI already built for exactly this purpose) marked everything "live," including several routes that don't exist anywhere in this migration yet (`/find-my`, `/the-mirror`, `/assessments/dharma-finder`, `/assessments/consciousness-scale`, `/assessments/grant-study`, `/flow-circuit`) — corrected each item's status to this app's actual current build state rather than shipping false LIVE labels on what would be 404s. **Real completeness fix**: added Find Your Peptide and Find Your Sexuality to the directory (Body & Temple and Love & Belonging) — both are real, fully-built assessments elsewhere in this migration that didn't exist yet when legacy was written; updated the "26 doorways"/stats-row counts to the real 28-item total (22 live, 6 coming). `/the-index` (referenced in one wound-card link) repointed to `/search`, the same fix already made for the identical dead link in `app/series/page.tsx`. **Real accessibility fix**: the 8 "What Are You Healing From?" wound cards' real wound/subtext/links content is unchanged, but legacy's hover-to-reveal interaction (imperative `onMouseEnter`/`onMouseLeave` DOM manipulation, no click handler) meant the links were completely unreachable on any touch device — links are now always visible. Legacy's dead Manus hero image (`/api/img/findme-orig_c4cf916c.jpg`) dropped for the plain `ThemedBackground` (theme="selfportrait") the page already rendered behind everything anyway. `trpc.assessments.submit`, its `sessionId` generator, and the `mirrorData`-based article-title lookup (whose own ternary computed the identical slug-title-case string on both branches — dead code either way) were all dropped; real article titles are now fetched server-side via `postsBySlugsQuery` and passed into the client component, more accurate than a naive title-case (which mangles real acronyms like "GDP"). `SacredGeometryBg` dropped, same decoration call made throughout this migration. Real Twitter/LinkedIn/email/clipboard share row (including the "10 shares" counter and "You're a Tribe Weaver already" copy) kept as a real backend-free feature. `AssessmentResultActions` simplified to this migration's real signature. Note: `/find-my` itself (the "Diagnose" door already linked from the homepage's Four Doors) is a **separate, not-yet-built `FindMyHub` component** in legacy — distinct from this page — still a real gap tracked for future work. `/find-my-me` and `/discover` → `/find-your-me` redirects added. Verified live: 200 OK, both redirects confirmed 308, Peptide/Sexuality confirmed present in the rendered directory, typecheck/lint clean.
- [x] `/find-your-therapy` (+ `/find-my-therapy`). Real 25 questions across 6 dimensions (Cognitive/Somatic/Relational/Depth-Unconscious/Altered States/Integrative), 6 real therapeutic modalities (each with a real description, "how it works," best-for/not-ideal-for lists, real named pioneers, and real book/directory resources), and the real archetype-matching logic (highest-scoring dimension wins) ported unchanged and verbatim. **Three real stale-count bugs found and fixed**: legacy's own file-header comment claimed "10 questions," its `AssessmentIntro` stats prop claimed 20 — neither matches the real 25-item array (the scoring code's own `maxPossible = 30 // rough max per dimension with 25 questions` comment confirms 25 is correct) — replaced both with live `QUESTIONS.length`. **Real dead-link bug fixed**: legacy's "Journey Continues" grid linked "Find Your Spirit"/"Find Your Religion" to external `findmyassess-9eekxcob.manus.space` stub URLs (dead Manus domain) despite this migration already having real, fully-built internal pages at `/find-your-spirit` and `/find-your-religion` — repointed to the real internal routes, and corrected Religion's stale "15 Questions" badge to the real 20. **Real accent-color inconsistency fixed**: legacy passed `accentColor="#7B1FA2"` only to the landing screen, then hardcoded brand-gold everywhere on the results screen instead — threaded one `ACCENT` constant through intro/radar/result-actions consistently, matching every other assessment in this migration. The "Also Worth Exploring" related-modality cards were dropped — in legacy, clicking one silently restarts the whole assessment instead of navigating anywhere related, and with no per-modality page to link to that would just be a misleading button. **Real cross-cutting contrast bug found and fixed in the shared `email-gate.tsx` component** (affects every assessment that uses it, not just this one): its cream text colors (`rgba(232,228,220,*)`, meant for a dark page) were nearly invisible against `ThemedBackground`'s actual light pastel themes (e.g. `#E8E4DC` on sake's `#F0E8DC` — two nearly-identical light colors) — replaced with the same dark-ink palette `AssessmentIntro` already uses on the same background. The real Twitter/LinkedIn/email/clipboard share row is genuinely backend-free and was ported as a real feature. `SacredGeometryBg` dropped, same decoration call made elsewhere. `/find-my-therapy` → `/find-your-therapy` redirect added. Verified live: 200 OK, redirect confirmed 308, typecheck/lint clean.
- [x] `/find-your-sake`. Real 20 questions across 5 dimensions (Palate/Ritual/Terroir/Philosophy/Season) and the real 5 sake profiles (each with real Japanese name, tasting notes, food pairings, serving temperature, named producers with region/note, and further-reading resources) ported unchanged and verbatim. Legacy's own `AssessmentIntro` stats prop claimed "15 questions, 6 dimensions" — didn't match the real 20/5 implementation — replaced with live `QUESTIONS.length`/`DIMENSIONS.length` counts, same fix pattern already applied on `/find-your-attachment-style` and `/find-your-sexuality`. **Two real bugs found and fixed**: (1) legacy's "Journey Continues" list read "Find Myzcal" — a literal typo for "Find Your Mezcal" — corrected. (2) legacy's JSX rendered `<WhatsNext />` (a full page section) nested *inside* the small "Retake the Tasting" `<button>` element — moved to a proper top-level section, same class of nested-interactive-content bug already fixed on `/find-your-religion`. The dark `GlassPanel` treatment (near-black translucent cards with light cream text, sitting on the page's own light parchment `ThemedBackground`) was kept as a real, intentional design choice rather than "fixed" to this migration's usual light-card convention — unlike the light-text-directly-on-light-background contrast bugs found on Style/Sexuality/Peptide/Religion, the text here sits inside its own correctly-contrasted dark card. `SacredGeometryBg`'s canvas-based floating rice-grain particles dropped (same decoration-not-worth-the-JS-cost call made repeatedly elsewhere). No `/find-my-sake` alt path exists in legacy routing (only `/find-your-sake`), so no redirect was needed. Verified live: 200 OK, typecheck/lint clean.
- [x] `/find-your-spirit` (+ `/find-my-spirit`), along with a shared PDF-export component the rest of Phase 9 can reuse. Real 35 questions, 10 dimensions, and the 6 real spiritual traditions (each with real description/practices/key texts/figures/modern expressions/growth edge) ported unchanged and verbatim — legacy's own file-header comment claimed "12 archetypes," but only 6 `TRADITIONS` actually exist in the code; ported the real 6, not the stale doc comment. The real weighted tradition-matching logic (top 3 dimensions per tradition, weighted 3/2/1) is unchanged. Ported `components/assessments/result-actions.tsx` from legacy's `AssessmentResultActions` — kept only the real, backend-free "Download PDF" action (browser print); the "Send to Tony"/"Keep Private" buttons posted to an unbuilt `trpc.assessments.submit` and weren't reproduced, same honest-degradation pattern used throughout this migration. Verified live: 200 OK, redirect confirmed 308, typecheck/lint clean.
- [x] `/find-your-religion`. Real 20 questions across 8 dimensions, 8 real spiritual/philosophical archetypes (each with real description/practices/key texts/figures/resonant traditions/growth-edge tensions), and the real scoring/matching logic ported unchanged and verbatim. **Real bug found and fixed**: legacy's own JSX rendered `<AssessmentResultActions>` nested inside the "Retake Assessment" `<button>` element — a button inside a button, invalid HTML — fixed by rendering it as a sibling instead, same fix pattern already applied on `/find-your-spirit`. Verified live: 200 OK, typecheck/lint clean.
- [x] `/find-your-diet` (+ `/find-my-diet`). Real 15 questions, 6 real archetypes (with descriptions), and the real nearest-neighbor archetype-matching logic ported unchanged. Legacy's dead `JourneyTracker` import wired in for real (see `journey-tracker.tsx`). `/find-my-diet` → `/find-your-diet` redirect added. Verified live: 200 OK, full quiz flow, redirect confirmed 308, typecheck/lint clean.
- [x] `/find-your-movement` (+ `/find-my-movement`), along with the shared assessment foundation the rest of Phase 9 depends on. Ported `components/assessments/{themed-background,assessment-intro,email-gate,whats-next}.tsx` from legacy's `ThemedBackground`/`AssessmentIntro`/`EmailGate`/`WhatsNext` — real content/behavior unchanged, with 2 real fixes along the way: (1) `EmailGate` now posts to the real `/api/subscribe` Route Handler (built earlier this migration) using the exact `find-your-<slug>` source keys already defined in `lib/content/kit-source-tags.ts`'s `KIT_TAG_MAP` — legacy's own `assessment-gate:<slug>` prefix didn't match any tag in that map, so this is a genuine improvement, not just a port. (2) `ThemedBackground`'s per-theme decorative particle field + SVG sacred-geometry overlay + 2 extra radial-glow layers dropped (same "not worth the cost" call made repeatedly elsewhere) — the real per-theme base gradient (16 themes) is kept, precomposed as one literal Tailwind class per theme rather than assembled from fragments. `find-your-movement-quiz.tsx` ports all 15 real questions and the real 6-dimension (Strength/Flexibility/Endurance/Mindfulness/Community/Adventure) scoring unchanged; the legacy `JourneyTracker`/`useJourneyProgress` import in the source file was dead code (imported, never rendered) and not ported. `/find-my-movement` → `/find-your-movement` redirect added (`next.config.ts`), same duplicate-route pattern as `/find-my-tribe`. Verified live: 200 OK, full quiz flow renders, redirect confirmed 308, typecheck/lint clean.
- [x] `/find-your-sleep` (+ `/find-my-sleep`). Real 15 questions, 6 real archetypes, and the real cascading archetype-match logic ported unchanged, including legacy's own real behavioral quirk of rendering `JourneyTracker` on every phase (not just results). One real duplicate-destination "Journey Continues" link pair dropped (see file's own port note), the one real distinct link (Find Your Diet) kept. `/find-my-sleep` → `/find-your-sleep` redirect added. Verified live: 200 OK, redirect confirmed 308, typecheck/lint clean.
- [x] `/find-your-coffee` (+ `/find-my-coffee`). Real 15 questions, 6 dimensions (Roast Preference/Origin Curiosity/Brewing Ritual/Social Context/Caffeine Sensitivity/Flavor Complexity), 6 real archetypes (each with a real description and 5 real named coffee recommendations — roaster, origin, roast level, tasting notes, real "why" rationale), and the real archetype-matching logic (sum of each archetype's 2 primary dimensions, highest wins) ported unchanged and verbatim. This legacy page predates the shared `ThemedBackground`/`AssessmentIntro` component system the rest of Phase 9 is built on — it hand-rolled its own hero-image page shell — so it needed more normalization than most: its hero image (`/api/img/coffee-orig_a935f111.jpg`) was a dead Manus asset, dropped for the standard `ThemedBackground` light theme (added a new "coffee" warm-cream entry) instead of reproducing a broken background; the canvas bean-particle field, floating steam-wisp divs, and SVG sacred-geometry overlay all dropped (same decoration-not-worth-the-JS-cost call made repeatedly elsewhere). **Real bug found and fixed**: legacy read `window.innerWidth` directly during render (twice — to pick grid columns and flex direction), unsafe during SSR and a real hydration-mismatch source — replaced with responsive Tailwind classes. `EmailGate`'s prop was legacy's `assessmentName`; ported using this repo's real `assessmentSlug="coffee"` to match `find-your-coffee` in `KIT_TAG_MAP`. Legacy imported `useJourneyProgress` for `markComplete` but never rendered a visible `JourneyTracker`, and never called `AssessmentResultActions` at all (it didn't exist yet when this legacy page was written) — both added for consistency with every other assessment in this migration. The 3 real "Journey Continues" links (Diet/Movement/Sake — genuinely distinct destinations, not a duplicate-link bug) were kept. `/find-my-coffee` → `/find-your-coffee` redirect added. Verified live: 200 OK, redirect confirmed 308, typecheck/lint clean.
- [x] `/find-your-kitchen`. Real 15 questions (all rendered on one page at once — a genuinely different, legacy-intentional UX from the other paginated quizzes, kept as-is) and the real dual-tracking scoring (answer score keys split into archetype tallies vs. 6 separate radar-chart dimensions) ported unchanged, including the 3 real "Journey Continues" links (one to already-built `/find-your-diet`, one forward-reference to `/find-your-style`). Verified live: 200 OK, typecheck/lint clean.
- [x] `/find-your-style`. Real 15 questions, 6 real archetypes, and the real (if crude — legacy's own comment admits "Simple mapping for demonstration") threshold-based archetype match ported unchanged. **Two real inconsistencies found and normalized**: legacy's own inline styles used near-illegible light text on `ThemedBackground`'s actual light pastel "style" theme (inconsistent with every other assessment in this migration); and legacy rendered `<JourneyTracker />` defaulting to `variant="dark"` on a light results screen instead of the `variant="light"` every other assessment passes. Verified live: 200 OK, typecheck/lint clean.
- [x] `/find-your-attachment-style` (+ `/find-my-attachment-style`, `/find-my-we`). Real 18 questions (legacy's own `AssessmentIntro` stats prop under-reported this as "15" — used the live `QUESTIONS.length` instead of repeating the stale number, same live-count discipline used elsewhere in this migration), the 4 real archetypes, and the real scoring/selection logic all ported unchanged, including the real closing note on attachment theory. Both alt-path redirects added. Verified live: 200 OK, both redirects confirmed 308, typecheck/lint clean.
- [x] `/find-your-love-language`. Real 15 questions (incl. the negative-score "disconnect" question), 5 real archetypes with descriptions/tips, and Dr. Gary Chapman's real attribution note all ported unchanged. Verified live: 200 OK, full quiz flow, typecheck/lint clean.
- [x] `/find-your-peptide` (+ `/find-my-peptide`) — "The Peptide Clarity Index™." A real, evidence-cited 10-question assessment across 7 clinical axes, mapping to 16 real peptide archetype profiles — every question (with its real PubMed/journal citation), every profile's real primary/secondary peptides with mechanisms and evidence, suggested lab work, red flags, and stacking notes ported unchanged and verbatim (clinical-adjacent content, not paraphrased). The real Peptide Readiness Score, contraindication-flag screening, share buttons (Twitter/LinkedIn/email/copy — real, backend-free), and the real medical disclaimer are all preserved. Real hero image (CloudFront, confirmed live) kept via `next/image`. Reused the simplified `AssessmentResultActions` (PDF-only) built for `/find-your-spirit`. Same light-text-on-light-background contrast bug already caught elsewhere was fixed here too. Verified live: 200 OK, redirect confirmed 308, typecheck/lint clean.
- [x] `/find-your-sexuality` (+ `/find-my-sexuality`). A research-informed assessment across 6 real dimensions, grounded in real cited research (Klein Sexual Orientation Grid 1978, Kinsey Scale 1948, Lisa Diamond 2008, Sari van Anders 2015) — real 16 questions (with per-question research context notes), real 12 archetypes (each with a real description + research note), and the real scoring/selection logic all ported unchanged and verbatim, including the real closing disclaimer. Legacy's own `AssessmentIntro` stats prop claimed "20 questions, 7 dimensions" — didn't match the real 16/6 implementation, used the live counts instead. Legacy's results screen also used light grey text on the real light pastel theme background (same low-contrast bug already caught on `/find-your-style`) — normalized to dark ink. Verified live: 200 OK, redirect confirmed 308, typecheck/lint clean.
- [x] `/find-my` — "Find What's Yours," the real destination of the homepage's "Diagnose" door (confirmed by grepping `lib/content/home-doors.ts`'s `/find-my` `href`) and of `whats-next.tsx`'s "Take Another Assessment" link, both already pointing here as forward-references before this page existed. Legacy's 26-item `ALL_LINKS` directory (Featured + 6 categories) ported unchanged and verbatim into `lib/content/find-my-directory.ts`. **Real accuracy fix, same pattern as `/find-your-me`'s own port note**: cross-checked all 26 destinations against the real route list — `/find-your-ev`, `/life-assessment`, and `/soulscore` (at the time) didn't exist yet, rendered as real "Coming Soon" cards instead of links that would 404 (dharma-finder/consciousness-scale/grant-study have since shipped and now flip to live — see below). **Real reuse, not re-derivation**: every item whose destination already exists in `find-your-me.ts`'s audited `DIRECTORY` pulls that entry's real `hook` text and `status` at build time via a lookup helper, instead of hand-copying a second, driftable copy of the same facts. **Two real live destinations added that legacy linked but `find-your-me.ts`'s directory didn't track**: `/brewsoul` (confirmed 200) and the external `findmystem-s3lknc4h.manus.space` sibling tool (confirmed live), both using the same "real external sibling product" trust already established for the other `manus.space` tools. Legacy's dead Manus hero image dropped for `ThemedBackground` (theme="ecosystem"), same call as `/find-your-me`. `AssessmentProgressBar` (never ported) replaced with the real `JourneyTracker` (compact). Ships as a pure Server Component. Verified live: 200 OK, exactly 1 header/footer, real content + live links confirmed in rendered HTML, typecheck/lint clean.
- [x] `/assessments` — "Three Maps. One Journey," confirmed genuinely distinct from `/find-my` rather than a duplicate: `/find-my` is the wide 26-item ecosystem directory, `/assessments` is its own page for exactly 3 long research-grounded instruments (Dharma Finder / Consciousness Scale / Grant Study) that also happen to appear as one category inside `/find-my`'s directory — legacy kept these as two separate pages, so the port does too. Real title/subtitle/description/time/icon/accent content for all 3 instruments ported unchanged and verbatim into `lib/content/assessments-hub.ts`, including the full "Why These Three" closing essay (Schmachtenberger/Hawkins/Harvard Grant Study triangulation). All 3 instrument cards now link live (dharma-finder/consciousness-scale/grant-study shipped alongside this page). Legacy's footer claim "Results saved for your return" was dropped — replaced with an honest line since nothing persists results yet. Added a real cross-link to `/find-my`. `JourneyTracker` reused unchanged; no `EmailGate`/`AssessmentResultActions` — pure preview/hub page with no quiz or results step of its own. Verified live: 200 OK, exactly 1 header/footer, real content in all 3 cards, typecheck/lint clean.
- [x] `/dharma-finder` (+ `/assessments/dharma-finder` redirect). Structurally different from every other Phase 9 quiz — legacy's real 25 questions are open-text reflection prompts, not multiple choice, distilled from Daniel Schmachtenberger's Dharma Inquiry and grouped into 4 real sections (Capacities/Values/Propensities/The Shadow Inquiry), each with a real framing quote, plus 3 real interstitial quotes shown between sections with the original 4-second auto-advance. All 25 questions, all quotes, and legacy's own keyword-matching `generateDharmaProfile` (deriving up to 3 archetypal roles and up to 4 core values from free-text answers, plus a completion-depth percentage) are ported unchanged and verbatim into a new `dharma-finder-quiz.tsx`. **Real bug found and fixed**: legacy's result-screen JSX rendered `<AssessmentResultActions>` nested inside the "Retake Assessment" `<button>` element — a button inside a button, invalid HTML — fixed the same way as `/find-your-religion`. **Real dead-link bug fixed**: "Journey Continues" linked to `/assessments/consciousness-scale` (repointed to the real `/consciousness-scale`) and `/assessments/grant-study` (dropped at the time, now live too). `EmailGate` (new here; legacy had none) is a real ecosystem-consistency addition. `find-your-me.ts`'s ecosystem-directory status flipped from "coming" to "live" now that this page ships. Verified live: 200 OK, typecheck/lint clean.
- [x] `/consciousness-scale` (+ `/assessments/consciousness-scale` redirect). Real 25 questions, each scored against David R. Hawkins' real 17-level Map of Consciousness (Shame 20 through Enlightenment 700+, final score = average of all 25 answers) — every question/option, every level's name/emotion/threshold, and `getInsight`'s exact 6 tiers of interpretive copy ported unchanged and verbatim into a new `consciousness-scale-quiz.tsx`, along with the real live running-average indicator, visual scale gauge, full 17-level map, 6-month reminder, and X/LinkedIn/copy share. Since this assessment produces one composite score on a single axis rather than independent dimensions, a bespoke single-axis gauge replaces `AssessmentRadarChart`; per-level colors (17 known-at-build-time values) are precomposed literal Tailwind classes rather than assembled from hex fragments. **Real Manus-dependency bug found and fixed**: legacy's cinematic Q1 hero used a `/api/img/consciousness-scale-hero-v2_085e0746.jpg` Manus-proxied image — forbidden even temporarily per this migration's Zero-Manus rule — dropped for the plain question-card layout every other assessment uses; a real Sanity-hosted replacement is a tracked follow-up (see `BLOG-IMAGE-BRIEFS.md`-style tracking). `find-your-me.ts`'s ecosystem-directory status flipped from "coming" to "live" now that this page ships. Verified live: 200 OK, typecheck/lint clean.
- [x] `/grant-study` (+ `/assessments/grant-study` redirect) — "Find Your Score," the real 25-question (5 per factor), 5-factor Harvard Grant Study life-satisfaction assessment (724 men, 1938–present, 85+ years — the longest-running study of human happiness). Every question, the 5 real factors (Warm Relationships/Adaptive Coping/Generativity/Career Satisfaction/Physical Vitality), every per-factor/per-tier insight (real named findings — Vaillant's "mature defenses," Erikson's generativity, Waldinger's quote), and the closing "Tony's Note" (Vaillant's "Happiness is love. Full stop." + Waldinger's "good relationships keep us happier and healthier") all ported unchanged and verbatim. **Real nested-interactive-element bug found and fixed**: legacy rendered `<AssessmentResultActions>` (its own `<button>`) literally inside the "Retake Assessment" `<button>` — same bug class already caught on `/find-your-religion`/`/find-your-sake` — split into siblings. **Real dead-link fix**: "Journey Continues" pointed "Find Your Spirit" at a dead external Manus stub despite the real internal `/find-your-spirit` existing — repointed. **Real ecosystem-consistency normalization**: unlike every sibling "Find Your X" quiz, legacy skipped `EmailGate` entirely and went straight to results — added for consistency, plus new `find-your-grant-study`/`find-your-dharma-finder`/`find-your-consciousness-scale` alias keys in `kit-source-tags.ts`'s `KIT_TAG_MAP` so `EmailGate`'s fixed `find-your-<slug>` source prefix actually resolves to the intended tag instead of a generic fallback. `resultSlug="grant-study"` passed to `AssessmentResultActions`; reuses the existing `find-your-score` `JourneyTracker`/`JOURNEY_MAP` id (repointed from the old `/assessments/grant-study` URL to the real route). `find-your-me.ts`'s ecosystem-directory status flipped from "coming" to "live." Verified live: 200 OK, real content confirmed in rendered HTML, typecheck/lint clean.
- [x] `/soulscore` — SoulScore™, ImpactSoul's 12-dimension impact measurement engine (1231-line legacy file, the largest remaining port). Unlike the quiz-style "Find Your X" assessments, this is a live interactive calculator, not a Q&A flow: entity-type picker, 12 weighted dimension sliders (Hawkins-scale Consciousness + 11 others), real-time score/grade/radar/auto-diagnosis, a benchmark table against 7 real entities (Patagonia, BP, Fortune 500 Avg, etc.), a 6-tier/30-factor supply chain drill-down, 20 "why every impact system failed" entries, a real Consciousness-Adjusted NPV calculator, and the gig-economy 5-layer stack comparison — all real content/formulas ported verbatim. Fixed: legacy's own SEO block described an unrelated assessment (stale copy-paste, replaced with real metadata); a div-pretending-to-be-a-button wrapping 30 nested range-input sliders (fixed to a real `<button>` + sibling content div); hardcoded intro/tab counts now read off real array lengths. No `EmailGate` (legacy never had one here — it's an open calculator, not a report unlock), matching `/find-your-peptide`'s precedent. `AssessmentResultActions` wired with `resultSlug="soulscore"`. `journey-tracker.tsx`'s `JOURNEY_MAP` and `/impact-dashboard`'s "Launch SoulScore Engine"/"Launch C-NPV Calculator" links (previously a disabled "Coming Soon" note) now point at the real page. This closes the real dead-end referenced from `site-nav-data.ts` (`/soulscore` appears twice in nav) and from `/impact-dashboard` — the last item in the `STATUS.md` dead-end list below.
- [x] `/self-portrait` — composite of completed assessments. Decision made: **local-only** (a real cookie, not Supabase — Phase 2 isn't provisioned yet). `AssessmentResultActions` (`components/assessments/result-actions.tsx`) takes an optional `resultSlug` and logs completions to a `tg_assessment_results` cookie via `lib/assessments/result-log.ts`, the same real-cookie-read-via-`next/headers` pattern already established by `returning-visitor-hero.tsx`/`track-last-blog-visit.tsx` — the page itself is a Server Component reading that cookie with zero client hydration step. **Real, deliberate scope cut, not a silent one**: legacy's `SelfPortrait.tsx` combined each assessment's own per-dimension *scores* (16 separate localStorage keys) into one composite radar chart across 15 unified dimensions, gated behind "5+ completed." This migration's result log only ever records that an assessment was completed, not its actual scored answers — building the real composite radar would mean threading a second, much larger data channel through every quiz. Shipped the honest subset instead: a real completed-assessments list (title, real completion date, retake link) plus a real "X of N completed" count and a "Still to Explore" section for the rest, via a new `lib/content/self-portrait-catalog.ts`. Wired `resultSlug` into the 7 remaining older "Find Your X" quizzes that already call `AssessmentResultActions` but hadn't been threaded yet (spirit/therapy/religion/sake/coffee/peptide/me) — `/self-portrait` now tracks 13 real assessments total. `/find-my`'s "Self-Portrait" teaser CTA (previously a real "Coming Soon" badge) now links to the real page. The remaining 8 "Find Your X" quizzes (attachment-style/diet/kitchen/love-language/movement/sexuality/sleep/style) never rendered `AssessmentResultActions` at all in legacy — wiring those in is a bigger, separate change (adding the whole action bar, not just a prop) and is tracked as a real follow-up, not done here. Verified live: 200 OK on both the empty state and a real cookie-driven completed state (curl with a synthetic `tg_assessment_results` cookie confirmed the real title/summary/completion-date/retake-link render, and the count reads "1 of 13 completed"), typecheck/lint clean across the whole repo.
- [x] `/the-mirror` (+ `/life-assessment`) — uses `mirrorData.json`, real 6-dimension/18-question life assessment ("The Mirror") ported into a typed `lib/content/mirror-data.ts`. Legacy served the identical component at both paths; `/the-mirror` is canonical (it's the only URL every existing internal link already pointed to — nav, journey tracker, Find Your Me), `/life-assessment` permanently redirects there. Real dimension-averaged scoring, radar chart via the shared `AssessmentRadarChart`, and weakest-dimension article/satellite-site recommendations ported unchanged. A live Sanity check found only 27 of 85 legacy article slugs still resolve; recommendations now resolve server-side and defensively skip missing ones, matching legacy's own behavior. Fixed: a hydration-unsafe `typeof window` branch computing the share URL, three stale `manus.space` satellite URLs superseded by their current live counterparts already used elsewhere in this migration, and dead `/assessments/*`/external-stub links in the "Journey Continues" grid. Added `find-your-mirror` to `kit-source-tags.ts`'s `KIT_TAG_MAP` and reconciled `journey-tracker.tsx`'s stale 10-minute estimate to legacy's real "~4 minutes" claim. `find-your-me.ts`'s ecosystem-directory status flipped to "live." Verified live: 200 OK on both paths, redirect confirmed, typecheck/lint clean.
- [x] `/assessment` — confirmed via full legacy read this is *not* a "Find Your X" quiz (it predates that convention): it's Tony's own 3-way reader-path diagnostic — Builder / Crusader / Investor — 5 real questions, one point per matching answer, highest tally wins. Real questions and real archetype copy ported unchanged, reused from `lib/content/archetypes.ts` (already ported for `/essays`'s archetype filter) rather than duplicated — added the page's `ASSESSMENT_QUESTIONS` to that same module since both come from the identical legacy source file. Preserved the real, distinct UX (no landing screen, no `ThemedBackground`/`JourneyTracker` — this page sits outside that ecosystem). **Real bugs fixed**: JS `onMouseEnter`/`onMouseLeave` inline-style hover hack replaced with Tailwind `hover:` classes; literal `"..."` pending-state text replaced with a real `Loader2` spinner per house rule; dead `trpc.subscribe.add` replaced with a real `POST /api/subscribe` call. **Real link audit**: kept `/impact-dashboard`, `/find-your-me`, `/living-declaration`, and the real `/blog/boiling-the-human-summit-harvard-kurzweil` article as live links; `/soulscore` and `/humanos` are established forward-references used repeatedly elsewhere in this migration and both now ship too. The Human OS CTA reuses the `border-red-800/30 text-red-800` treatment `/living-declaration` already established for the identical button instead of a new one-off raw-hex pair. `AssessmentResultActions resultSlug="assessment"` added (didn't exist when legacy was written). Added `assessment-builder`/`assessment-crusader`/`assessment-investor` keys to `kit-source-tags.ts`'s `KIT_TAG_MAP` for real per-archetype subscriber segmentation. `useBehavior`'s legacy localStorage archetype/pageview tracking has no equivalent anywhere else in this app and wasn't reproduced. Verified live: 200 OK, real content confirmed in rendered HTML, typecheck/lint clean.
- [x] Fix dead-end pages flagged in `STATUS.md`: sake, therapy, and love-language all got real next-step navigation (`WhatsNext` + real "Journey Continues" links) during their ports — the last remaining one, `soulscore`, is now built (see above), closing this out.
- [x] Port shared radar-chart / PDF-export / email-gate components used across assessments — `components/assessments/{radar-chart,result-actions,email-gate}.tsx`, built during Phase 9 and reused unchanged by all 15 "Find Your X" quizzes plus `/find-your-me`.

## Phase 10 — FauxTony Chatbot

- [ ] Netlify-style route handler → Next.js Route Handler at `web/src/app/api/chat/route.ts`, proxies to Claude API (`@anthropic-ai/sdk`), API key server-side only via env var
- [ ] Content-grounding index built from Sanity content (mirrors legacy `SITE_PAGES`/`searchablePages.ts` approach) passed as context
- [ ] Chat widget component (`web/src/components/faux-tony/`) — no new animation dependency, CSS transitions only
- [ ] Session memory (client-side session id + Supabase-logged history)
- [ ] Log every conversation turn to Supabase `chat_conversations`/`chat_messages`
- [ ] Cost controls: prompt caching on grounding content, capped `max_tokens`, fast/cheap model tier by default
- [ ] Port 3-question-limit / "Tony's voice" system prompt behavior from legacy `tonyKnowledgeBase.ts`
- [ ] `/shared-chat/[shareId]` — read-only shared transcript viewer

## Phase 11 — Publish-Approval Workflow

- [ ] Internal dashboard `web/src/app/(dashboard)/approvals/page.tsx`, gated by Supabase Auth
- [ ] List pending `change_requests` with preview links
- [ ] "Approve & Publish" action: Server Action that (a) publishes the Sanity draft via Sanity's mutate API, or (b) triggers the relevant deploy, then updates the Supabase row to `published` with timestamp + approver
- [ ] Webhook/Server Action that creates a `change_requests` row whenever a Sanity draft is created (Sanity webhook → Next.js Route Handler)
- [ ] Full audit trail view (who approved what, when)

## Phase 12 — SEO Hardening, Redirects & Cutover Prep

- [ ] Per-page `generateMetadata` (title, description, OG, Twitter Card, canonical) for every ported route, sourced from Sanity `seo` object where content is CMS-backed, static otherwise
- [ ] `sitemap.ts` generated from real Sanity content + static route list, auto-updates on publish
- [x] `llms.txt` — fetched verbatim from the still-live site and saved to `web/public/llms.txt` (real content: Tony's bio, expertise, full article index by category, every product/tool section) — do this before Manus goes away, it won't be recoverable after
- [x] `robots.txt` — same approach: fetched the real live file (not my earlier guessed `robots.ts`, which has been removed) and saved verbatim to `web/public/robots.txt`, plus added `Disallow: /studio` for the new Sanity Studio route. Confirmed live: real AI-crawler allowlist (GPTBot, Claude-Web, PerplexityBot, etc.), scraper blocklist, and the site's non-standard `LLMs:` directive all intact
- [x] JSON-LD: `WebSite` + `Person` schema ported verbatim from the live homepage's actual `<script type="application/ld+json">` tags (not reconstructed from memory) — rendered site-wide from `src/lib/structured-data.ts` in the root layout. The Person `image` was hardcoded to a `/api/img/...` legacy proxy path on the live site — rescued that headshot (downloaded it while still live, uploaded to Sanity as the `author-tony-greenberg` document's avatar) so the new site's JSON-LD has zero Manus dependency instead of just copying the fragile path forward
- [x] `Article` JSON-LD on `/blog/[slug]`, built from real per-post Sanity data (headline, image, datePublished/dateModified, author, canonical URL) — verified live on a real post
- [ ] Build the full Manus/legacy-URL → Next.js-URL 301 redirect map (`next.config.ts` `redirects()` or Sanity `redirect` documents) — **hard QA gate, nothing proceeds to DNS cutover until this is complete and tested**
- [ ] Visual diff pass: every ported page vs. legacy page
- [ ] Link check across all ported pages (no 404s, no broken internal cross-references)
- [ ] Lighthouse pass (performance/accessibility/SEO) on top 20 pages by traffic
- [ ] Connect Google Search Console + GA4 to the new deployment on day one of cutover
- [ ] Restore/adapt the manus.space triple-layer noindex lockdown pattern for any remaining staging URLs

## Phase 13 — Images Needed in Sanity

**⚠ The legacy Manus-hosted site (tonygreenberg.com's old backend) is now fully down — confirmed 503 on the root domain and 404 on every `/api/img/` path as of this pass, not just "will be decommissioned soon."** Every earlier "rescue before it disappears" image pull already done is safe (uploaded to Sanity), but **no further image rescues from the legacy host are possible** — any image reference still pointing there for a not-yet-ported page/post is now permanently lost, not just at risk. Treat every remaining item below as "source new art" from here on, not "go fetch the old one."

Every image below needs to exist as a real asset uploaded to Sanity (or explicitly kept as a static `public/` file if it's a UI/brand asset, not content). Fill in as each is sourced/uploaded — do not launch with placeholder images.

- [x] Favicon — real "G" brand mark generated from the header's actual wordmark treatment (`src/app/icon.tsx`), see Phase 3
- [ ] Site-wide: logo (light + dark) beyond the current text wordmark, default/fallback OG image (1200×630)
- [ ] Author photo(s) for blog `author` schema (Tony Greenberg headshot at minimum)
- [x] Blog hero images — audited, then migrated. Final state:
  - [x] 22 posts have a real hero image, now live in Sanity (17 already on RampRate's own CloudFront + 4 rescued from the legacy `/api/img/` proxy + 1 rescued from `/manus-storage/`, all pulled over by `migrate-blog-posts.ts` before Manus is decommissioned)
  - [ ] **99 posts still need a real, unique hero image** (98 previously pointed at the generic `og-default.jpg` fallback; 1 had no image field at all). Tracked one row per post, with a suggested image-generation prompt, in [`BLOG-IMAGE-BRIEFS.md`](../BLOG-IMAGE-BRIEFS.md) — work through that file and check items off there, not here. No image-generation tool was connected in this environment as of this writing; either connect one (e.g. an MCP image-gen extension) or source these manually/with a designer, then upload via Sanity Studio (`/studio`).
- [ ] Blog: per-post OG image if different from hero (many posts can reuse hero as OG)
- [ ] Marketing pages: hero/section images for `/the-letter`, `/the-territory`, `/engine-room`, `/the-body`, `/the-nightstand`, `/recent-creations` (portfolio thumbnails), `/published` (publication logos), `/clients` (client logos — confirm licensing before re-hosting)
- [ ] `/akbar` essay images (~20 photos — hero mosaic, JC portrait, full-bleeds, food grids, footer gallery) — **confirmed permanently lost**, not just at-risk: all were on the now-dead Manus `/api/img/` host (404/503). Needs a real photo shoot or licensed stock, not a rescue script.
- [ ] BrewSoul: coffee/product images (`brewsoul-coffees-expanded.ts` likely has image refs — audit), city guide header images, roaster directory photos
- [ ] PRI/Kava: medicine reference images (`medicine-images.ts`), facility images for `/facilitator-index`
- [ ] Manifesto/Attention Theft: any diagram/illustration assets used on the mega-page
- [ ] Assessment result-screen background art (flagged in `STATUS.md` as a known gap — 18/20 assessments currently have plain black backgrounds; sourcing themed art here also closes that gap)
- [ ] Chatbot: FauxTony avatar/icon
- [ ] Audit `client/public/` (including `alex-azzi/`, `brewsoul/` subfolders) for any static assets already sitting there unused that should be pulled in rather than re-created

## Phase 14 — QA & Launch

- [ ] Mobile responsive pass on every ported section
- [ ] Accessibility pass (WCAG AA contrast, 44px touch targets, keyboard nav) — same bar as legacy `QA_MASTER_PROMPT.md`
- [ ] No dead-end pages (every page links to 2+ others) — same bar as legacy `MASTER_QUALITY_PROMPT.md`
- [ ] Cross-browser check (Safari iOS in particular, per legacy performance notes)
- [ ] Remove `noindex` only when sign-off is given — do not launch indexable by accident
- [ ] Final stakeholder sign-off (Tony/Darryl/Kimberly) on the ported site before DNS cutover
- [ ] DNS cutover, legacy app decommissioned only after a stable overlap window

---

## Explicitly deferred to a later, separate phase (not in this migration pass)

- Admin dashboards (`/admin/*`, `/analytics`, `/pri-research` — no admin-auth system exists yet and its data source, `trpc.priCalibration.*`, was never built either)
- Stripe billing (`/shop`, `/subscribe` full checkout flow — Phase 4 ports these as static/informational only)
- Spam-tracking system (`/youve-been-reported`, `/spamtoast`, admin spam tooling)
- Supplier/vendor intake backend wiring (Phase 4 ports the form UI only, keeps pointing at legacy backend)
- **MCP content-editing server for this project** — the user wants an MCP server for tonyg-site similar to the existing "claude.ai Ramprate - Admin" MCP server (`~/Desktop/ramprate-ui`), which lets Claude edit that site's code and Sanity content directly (github read/write, Sanity query/create/patch, analytics/PR/code-quality checks) instead of a hand-built in-app admin page. Explicitly deferred — not started — until the current PRI migration (Phase 7) is further along; revisit as its own dedicated task then.
